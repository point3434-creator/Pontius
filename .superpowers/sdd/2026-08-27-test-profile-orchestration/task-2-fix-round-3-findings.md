# Task 2 fix round 3 — binding scoped rereview findings

Read `task-2-brief.md`, both earlier fix-round finding files, and the complete appended `task-2-report.md` first. Work only in `D:/Pontius-worktrees/orch-task2`. Use one implementation agent, strict RED-before-GREEN tests, and no subagents. Do not run repository payloads, emit/approve/write capability rows, touch OneDrive, commit, merge, or push. Preserve every finding already marked addressed. Append exact RED/GREEN commands, snapshot IDs, hashes, and self-review evidence to `task-2-report.md`, then leave the worktree frozen and uncommitted.

The binding review input is `task-2-fix2-review.patch` SHA-256 `1838d666b863d1fc42f6c6155f366401e988777f182159bcedf673378f8203e8`; its report SHA-256 is `d04e01385b79e1166156b6ec5f56eee2c6c2ea5bb62acccd26cb3419dedf6f34`. CodeRabbit reported zero issues on its five-file analyzer cross-check, but the independent targeted diagnostics below reproduced real omissions and take precedence.

Controller ruling after native Windows 11/NTFS experiments: ordinary `ReplaceFileW`, `FileRenameInformationEx`, hard-link, share-mode, and oplock compositions cannot simultaneously provide continuous existing-name visibility, in-place atomic replacement, and strict identity-CAS against a same-user process that ignores the writer protocol. Both replace primitives fail with sharing violation while the target denies delete sharing; allowing delete permits the exact lost-update race, and directory oplocks are advisory. Therefore the supported boundary is serialized cooperative Pontius writers: acquire one deterministic per-destination lock before snapshot validation and retain it through commit/rollback; retain the parent identity with delete sharing denied; deny external writes to staging/recovery/published objects; and detect/refuse any conflict observable at the boundary. Do not claim protection from an arbitrary same-user namespace writer. A stronger guarantee would require an ACL-owned broker, transactional store, or kernel component and is outside Task 2. Cost if wrong: an external process that deliberately ignores the deterministic lock can still replace a child name in the unavoidable kernel gap; silently claiming strict CAS would be worse, while adding a broker/store now would materially redesign every consumer.

## Transaction coordinator and resource ownership

1. Replace the split outer/direct finalization paths with one transaction coordinator. Prepare any final Git pathname proof or lease reacquisition first. While all rollback authorities and bound object handles remain live, perform one final logical sequence covering source/effective-attribute/Git lifetime, every output, and the same source/effective-attribute/Git lifetime again, then commit immediately. No lease close, new `_strict_git_snapshot`, callback, filesystem read, or other fallible/mutation-capable operation may intervene. Direct standalone, direct pair, and collector paths must use the same coordinator; the pair primitive itself must execute its second lifetime pass.

2. Bind Windows objects by role. Staging and recovery objects must deny external write/delete sharing for their full transaction lifetime and be byte/identity checked through retained handles immediately before publication or restore. A published output must retain an appropriate no-conflicting-share handle through final validation and commit. No unverified inode may ever be renamed into the destination.

3. Restore deterministic cooperative Windows CAS. Replace UUID lock names with a deterministic per-destination lock (and canonical ordered acquisition for a pair), acquired before destination/source validation and retained through final commit/rollback. Retain the parent with delete sharing denied, and retain the destination/staging/recovery identities as far as Windows replacement semantics allow. A second Pontius writer must fail or wait before validation and may never overwrite the first. Preserve continuous existing-target visibility and parent-directory anchoring. Add a test documenting that an uncooperative process ignoring the lock is outside the guarantee; do not express the impossible claim as a passing security property.

4. Make resource state truthful. Never clear a native handle from transaction state until close is confirmed. A delete-disposition request is not recovery disposal; disposal completes only when the relevant close succeeds and absence is verified. A fail-before-close error must not be treated like an exception injected after a successful close. Retain/retry ownership, surface cleanup failure, and preserve rollback where still possible. Add native Windows fail-before-close tests for staging/published, recovery, and directory-lock handles; assert exact destination state and zero `.tmp`/`.recovery`/`.lock` artifacts.

5. Repair POSIX finalization ordering. Do not close/clear the temporary or recovery identity that rollback requires before fallible destination-handle cleanup and recovery unlink/disposal complete. Add forced recovery-unlink and asymmetric-close RED/GREEN seams for exchange and non-exchange paths. Every precommit failure must restore the original (or remove an initially absent publication), aggregate cleanup failures without masking the primary error, and leave zero artifacts.

6. Preserve the round-2 wins: sealed-memfd-only POSIX Git launch, Windows Git ancestor lease, handle-relative parent anchoring, delete-pending cancellation before restore, ordinary POSIX absent publication, continuous existing-target visibility, body/cleanup aggregation, and ordinary pair rollback.

## Analyzer state and fail-closed completeness

7. Replace global alias collapsing with source-ordered reaching definitions/taint at each call site. Multiple assignments, chained callable aliases, callbacks, attributes/subscripts, and container-propagated receivers that may resolve to owner/scientific/CuPy/subprocess targets must either derive exact rows or emit explicit blockers. Branch merges must take exact equal states or become blocked ambiguity. Lock at least the reproduced `target = print; target = cp.arange; target(1)` and `backends = [np, cp]; backend = backends[1]; backend.arange(1)` cases at nonzero row-or-blocker counts.

8. Evaluate subprocess environment state per call and per control-flow path, not by globally sorting selected AST nodes. Support exact lexical assignment, `pop`, literal `update`, and `del`; preserve inherit-versus-replacement semantics. Alias mutation, unsupported mutation methods, dynamic keys/values, and divergent branch states must block. Add exact multiple-call and branch tests for assignment/pop ordering, `update`, `del`, and conditional divergence.

9. Make runtime bounds structurally complete and fail closed. Cover `match` cases/guards, exception-handler type expressions, all already approved compound statements, and nested repetition. A five-iteration call under `match` must derive five or block; a protected call in an exception type must not disappear. Reuse the complete sink/alias/bound analyzer recursively for literal child `-c` programs. Nested subprocesses and mixed receivers in child code must derive explicit child capabilities with correct descendant permission/bounds or produce an explicit parent blocker—never a parent-only row with zero blocker. Preserve helper multiplication, branch maxima, dynamic-repetition blockers, and the controller-ruled canonical bound of 6.

10. Make conditional-decorator provenance complete or fail closed. Resolve supported module aliases, class-local exact aliases, and other statically exact forms; reject any unresolved decorator that may reference `skipIf`/`skipUnless`. Add the reproduced `u = unittest`, class-local `conditional = unittest.skipIf`, and literal `getattr(unittest, "skipIf")` cases. None may silently default to both platforms.

11. Preserve addressed contract behavior: explicit scientific qname/action/return registry with no `invoke`/`opaque` fallback, duplicate per-test lifecycle rejection, role-specific subprocess/call schema combinations, exclusions, H32 exact-path loading, and all previous exact corpus/digest oracles after intentional regeneration.

## Required verification

- First freeze RED snapshots reproducing every numbered defect above, including the reviewer’s native Windows concurrent-destination, staging mutation, recovery mutation, fail-before-close recovery/lock, final source/attribute/output mutation, direct-pair final-lifetime, and POSIX finalization fault seams.
- Run no implementation change until the RED failures are confirmed to be caused by the intended invariant violation.
- After GREEN, regenerate ordinary zero-scope governance data only; both capability scopes must remain absent/all-zero and no capability review/approval/write command may run.
- Finish with fresh D:-local snapshots of the complete inventory/finalization suite, 53+ configuration tests, exact H32 gate, byte-stable ordinary `--write`/`--check`, AST/static/format/hash/status checks, `git diff --check`, and zero residual transaction artifacts.
- Freeze a new immutable delta from fix round 2 for the same two independent scoped rereviewers. No integration or commit is allowed until both return clean.
