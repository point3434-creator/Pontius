# Durable research artifacts

This tracked file makes the repository-owned artifact parent explicit.
One-shot owners still create each result with exclusive create and never
overwrite, repair, or retry a retained path.

ADR-0375 permanently closes
`gpu_occupied_card_quotient_staged_scaling_v1.jsonl`; that absent path must
never be created. Its additive successor owns a distinct v2 result identity.

ADR-0389 reserves `legal_river_quotient_cuda_consumer_v1.jsonl` for a future
source-sealed one-shot owner. The path remains absent at preregistration; no
private consumer call, placeholder, repair, or early journal is authorized.
