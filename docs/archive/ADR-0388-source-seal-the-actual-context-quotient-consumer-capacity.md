# ADR-0388: Source-seal the actual-context quotient consumer capacity

- Status: accepted source-sealed CuPy-free actual-context quotient consumer-capacity result; 58 independently expanded shape/dtype rows place the legal rank-175/width-176 bridge under the fixed host/device caps with forward storage dead before adjoint birth, and the complete exact ten-card 128+48 forward/fold/adjoint differential passes, while live allocation, device execution, every 45-card quotient value, resolver iteration, solve, action, 15-second result, quality result, truncation choice, blueprint result, and poker-strength claim remain unopened
- Date: 2026-08-25
- Follows: ADR-0387
- Config canonical-LF SHA-256: `7cc8fec2b6cb6b7b135f2f7ea5dd74e1a5a3cd4b4a2c48d2b6f8ddde141bb71d`
- Parent ADR canonical-LF SHA-256: `7406d73c779115a173b8186f29f998683c3f48fdb262f4a31bf1a7e9ca2754a7`
- Capacity source canonical-LF SHA-256: `f2f6ddf9a5bd7945a69ea9f97e9c95093f35d3508f1a0a36c4d2ed841a327073`
- Capacity controls canonical-LF SHA-256: `bc8332dd3233d5139c0bfbc8f924e65b949cd6a3df6e2429e42db9cc90fc1e3e`
- Front-Door-Kind: controller-v1
- Front-Door-Research: ADR-0280
- Front-Door-Process: ADR-0388
- Front-Door-Contract: ADR-0307
- Front-Door-Revoked: ADR-0281
- Front-Door-Active-Next: Prospectively preregister, before CUDA consumer source or any 45-card value, one actual-context device-consumer boundary that rebinds ADR-0386 and ADR-0388, admits contemporaneous host/device memory, implements the global 128+48 forward/fold/adjoint schedule with complete-label streaming and absolute ownership telemetry, differentially proves the device seam on bounded populations, records every failure before scientific work, and keeps resolver iterations, solves, actions, the 15-second wall, decision quality, truncation selection, blueprint work, and poker strength closed
- Front-Door-Blockers: no source-sealed or live-admitted actual-context CUDA consumer, no full-width actual-context quotient value, no general odd-chip or side-pot leaf automaton, no repeated-actor multiway existence result, off-tree opponent-action result, cross-street belief and certificate handoff, certified full-width river strategy bridge, sealed blueprint trainer/checkpoint/abstraction/slice-audit chain, trained blueprint, v0a/v0b integrated bot, preparation-bank filling result, frozen evaluation opponent pool, complete 15-second decision, production action width, or poker-strength result exists

## Question

Does ADR-0387's proposed legal-context consumer schedule survive independent
source derivation and exact bounded differential control without importing
CuPy, allocating a declared full-width workspace, or opening a 45-card
quotient value?

## Decision

Accept `legal_river_quotient_consumer_capacity` as the source-only answer. The
module replays ADR-0386's legal river bridge, compares every semantic and
numeric payload with an independent replay, expands all three aggregate bridge
classes into physical NumPy shape/dtype rows, validates the prospective
thirteen-class proposal, and derives every host/device phase total by sweeping
the 58 expanded row lifetimes. Aggregate byte totals are cross-checks only.

The source imports neither CuPy nor the permanently consumed literal-45 owner.
It contains no device entry point and executes no 45-card forward, fold,
adjoint, resolver, master, solve, or action path. Full-width dimensions appear
only as integer shapes on `NumericLifetimeRow`. A fresh `-B` child process
imports the module, builds the source report, deliberately rejects an
oversized recurrence, and observes both CuPy and the literal owner absent
before and after. This is source-path evidence, not live admission.

The inherited front-door trust chain remains explicit. ADR-0310 made native-
simplex robustness the next systems question. ADR-0311's directive is
Preregister the native-simplex robustness audit. ADR-0312's directive is Seal
the native-simplex audit compiler and corpora. ADR-0313's directive is Seal
the native-simplex audit runner before results. ADR-0314's decision is Retain
the native-simplex audit and reject the frozen gate. ADR-0315's directive is
Source-seal the artifact-only native-simplex gate correction. ADR-0316's
decision is Accept the corrected audit and bound replacement eligibility.
ADR-0317 separates solver classes; ADR-0318 binds HiGHS 1.12.0; ADR-0319
requires one public HiGHS-DS call per canonical task; and All 177 ordered
observations pass under ADR-0320. ADR-0321 preserves caller-owned legal
fallback, ADR-0322 returns research evidence or rejection with no action,
ADR-0324 remains value-unopened, and ADR-0325 was authorized exactly once.
ADR-0326/0327 govern the exhaustive bounded development-teacher; ADR-0328
retains it and the solver-free rebinder; ADR-0330 remains permanently closed;
ADR-0331's append-and-fsync discipline, ADR-0332's exclusive `xb` open, and
ADR-0333's No replacement sizing value was opened statement remain binding.
ADR-0334/0335 bind the 2,113-task non-replay chain; ADR-0336 records width
three; ADR-0337 owns the response-closed direct mechanism; ADR-0338 alone
records the selected development raise width; and ADR-0339 remains a finite
absence claim. ADR-0340's 192 prospective tasks remain distinct from
ADR-0341's 94 accepted one-call arms and ADR-0343's 126 confirmation arms;
ADR-0342 alone authorized the retained confirmation. ADR-0344/0345 own the
finite h4 responder-raise keystone. ADR-0346/0347 lead only to responder-row
growth; ADR-0348/0349 lead only to selector-window work. ADR-0350 opened
selector-stable affine integration; ADR-0351 replaced it with tie-aware legal
h4 affine envelopes; ADR-0352 is closed by ADR-0353; ADR-0354/0355/0356 own
the factorized exact face result; ADR-0357/0358/0359 own and close same-fixture
integration; and ADR-0360/0361/0362 alone own the untouched confirmation and
assessment. ADR-0363 and ADR-0365 remain consumed; ADR-0364 remains exactly
`GetProcessMemoryInfo failed`; ADR-0366 remains exactly
`representation_rejected_before_target_allocation` with zero target calls.
ADR-0367 through ADR-0384 own and close the quotient algebra, capacity,
bounded CUDA, staged, liveness, validation, and literal-target ladder.
ADR-0385 froze the legal bridge question; ADR-0386 source-sealed only its host
answer; ADR-0387 froze this source-capacity question; and ADR-0388 seals only
its source answer. No earlier owner is revived.

For machine-checked continuity, ADR-0317's directive remains Separate solver
classes and prioritize the certified sizing adapter. All 177 ordered
observations pass under ADR-0320, making the separate consumer eligible.
ADR-0326 and ADR-0327 govern the exhaustive bounded development-teacher
chain. ADR-0328 retains that exhaustive teacher and solver-free rebinder.
ADR-0334 and ADR-0335 bind the 2,113-task non-replay chain. ADR-0344 and
ADR-0345 own the finite h4 legal responder-raise keystone line. ADR-0346 and
ADR-0347 lead only to responder-row growth. ADR-0348 and ADR-0349 lead only
to selector-window work. ADR-0351 requires the tie-aware legal h4 affine-
envelope. ADR-0354 through ADR-0359 own the factorized face and affine
consumer chain. ADR-0380 freezes the complete ordered populations 10 and 22.
ADR-0383's owner was invoke exactly once and remains consumed by ADR-0384.
ADR-0388 imports neither that owner nor its target. The phrases exclusive
untouched legal h4, selector-window, 2,113-task, exhaustive bounded
development-teacher, response-closed direct mechanism, and caller-owned legal
fallback retain their prior meanings.

The exact historical continuity strings remain explicit. ADR-0328 retains the
exhaustive teacher and solver-free rebinder before the direct closed
finite-block greedy line. ADR-0351 requires the tie-aware legal h4 affine-envelope.
ADR-0352 remains closed before any fresh untouched tie-aware affine result.
ADR-0353 precedes ADR-0354's factorized exact active-set directional-face
diagnostic. ADR-0355's owner was invoke exactly once; ADR-0356 retains that
directional-face diagnostic before tie-aware affine integration. ADR-0357
requires an exclusive legal h4 owner, ADR-0358 was invoke exactly once, and
ADR-0359 requires a fresh value-unopened confirmation. ADR-0382 preregistered
the literal-45 config; ADR-0383 source-sealed it; ADR-0384 closed it.

## Provenance and physical row expansion

The bridge and topology identities remain respectively
`bfe0f1e768bf323fa7d8036ef05176d5e0373f712e4875dcf76eabb96532a702`
and
`b499a4e92a739ea2bdf1d64b1653d5c652b707e5b88d8959db7f17f82f8761a9`.
The source does not trust those stored digests alone. It independently replays
the legal cards, betting state, action-conditioned belief, terminal check,
axis mapping, full hand domains, automaton semantics, factorized factors and
masks, and every fixture array. Same-shaped changed terminal values, a changed
identity, or a mutable/noncontiguous payload rejects.

The 15,888,996-byte host consumer class expands into every transition array,
terminal vector, sunk scalar, mixture, unary, mode factor, pair map, source
pairing template, query mask, query hand-index, and unary-offset row. The
84,972-byte validation class expands into each automaton bond-state array and
each retained factorized mixture, unary, and hand-mask array. The separately
retained factorized-belief payload is 79,224 bytes. The 79,216-byte warm view
is correctly absent from the additive rows because it aliases resident unary
and mode-factor buffers. The device mirror independently expands to the same
15,888,996 numeric bytes; equality of the two totals does not merge their
ownership.

Together these produce 58 unique physical rows. Every row's numeric bytes are
exactly `prod(shape) * dtype.itemsize`; no Python object is priced as a numeric
array. Full compatible-query, full query-covector, full unique-source, 90-fold
record-expanded source-adjoint, separate source-coefficient, and separate
adjoint-aggregate allocations are absent and structurally rejected.

## Source-only capacity result

The host bridge remains resident through the source schedule, so the host peak
is 15,973,968 numeric bytes at `host_bridge`. This is 15,888,996 consumer bytes
plus 84,972 validation bytes. The 48-GB host cap and the 60-GB physical
capacity with an 8-GB reserve both pass by source arithmetic.

The device resident base is 82,997,916 bytes: the expanded bridge mirror, one
seven-entry `int64` cardinality-offset row, and one explicit 67,108,864-byte
library scratch allowance. The first forward source phase adds the
9,759,784,960-byte `9,531,040 x 128` recurrence, a 67,108,864-byte
`65,536 x 128` compatible chunk, 1,048,576 bytes of numerator/reach chunks,
and two Float64 result accumulators. The independently swept device peak is
therefore 9,910,940,332 bytes at `forward_slice_0_source` and remains equal
through both forward slices. It is 2,089,059,668 bytes below the fixed 12-GB
numeric cap and leaves 4,089,059,668 bytes after the frozen 2-GB reserve
inside the 16-GB physical minimum.

At `forward_release`, only resident state and the two result accumulators
remain: 82,997,932 bytes. The first adjoint query phase is 318,265,004 bytes;
after the bounded unique-source chunk is born it is 351,819,436 bytes. No
forward operator or fold row is live in any adjoint phase, and no adjoint row
is live at `forward_release`.

This passes only the frozen numeric-array and minimum-physical inequalities.
Contemporaneous host/device free memory is `None`; live admission is `None`.
Python/interpreter storage, allocator fragmentation, CuPy pool retention,
CUDA context/driver/module/page-table storage, kernel registers/local memory,
compiler-selected temporaries, and unrelated process activity remain named
exclusions. The 2.089-GB numeric margin is not an allocator guarantee or a
latency prior.

## Global feature and streaming result

The two globally indexed ranges remain exactly `[0,128)` and `[128,176)`.
All 175 source-state features occur once; global feature 175 is the reach
feature and is explicitly owned only by the second slice. Physical workspace
width is 128, while logical work is exactly 128+48=176 columns. There is no
second 48-column allocation and no false 256-column logical ledger.

Fold code accumulates each slice's partial numerator and the sole reach
contribution, then permits one conditional normalization only after both
slice ordinals are present. A slice-local conditional request fails closed.
Reversed slice processing is required to reproduce the same result.

Streaming integers have semantic types. Forward owns 65,536 labeled query
records. Adjoint query input owns 10,922 complete four-card occupancies, which
derive exactly 65,532 six-label records. Adjoint source output owns 32,768
six-card occupancies. Reusing the 65,532 adjoint record count as the forward
knob, cutting a six-label group, or changing the occupancy-derived record
count rejects. Forward chunks fold before reuse, and adjoint chunks consume
before reuse.

## Complete bounded exact differential

The independent control uses every one of the 210 six-card occupancies and
every one of the 210 four-card occupancies on ten cards, with six labeled
query records per occupancy for 1,260 records. Its engineered payload has
source rank 175 and width 176; features 0, 127, 128, 174, and 175 all carry
detecting mass. It is a seam fixture, not a ten-card poker value.

The monolithic forward and adjoint oracles directly scan disjoint masks and
do not call the quotient transform. The sliced side calls the retained exact
quotient independently for each global range. Forward rows, folded numerator
and reach, adjoint rows, normal and reversed slice order, 17-versus-19 forward
record partitions, 3-versus-5 complete-occupancy adjoint partitions, and
11-versus-13 source-output partitions are all exactly identical under
`Fraction` arithmetic. The exact transpose dot identity also passes.

The canonical monolithic forward-row SHA-256 is
`57f8a809041af0f02a371e0f19d3910301d7ba6585eeddc34e969da5bef234f2`;
the adjoint-row SHA-256 is
`edc515bc581585c0f3a6feb039b0c6796afdfba306b50d3a082de42b06b2b433`.
These digests report the exact controls but do not decide them. The engineered
fold has numerator and transpose dot `-128107591/29400`, reach `277249/100`,
and normalized value `-128107591/81511206`. The supplementary Float64
recombination error is `2.220446049250313e-16`, below the unchanged `2e-11`
ceiling. None of these synthetic numbers is a poker-context value, strategy
comparison, or quality result.

## Adversarial controls and pre-seal correction

Controls reject dependency and parent drift, same-shaped terminal mutation,
reordered/missing feature coverage, slice-local state identity, duplicate
reach ownership, per-slice normalization, a cut label group, forward/adjoint
knob reuse, an omitted or duplicated lifetime class, element/byte unit
confusion, a changed birth/death edge, forward/adjoint co-residency, a
forbidden full array, and an oversized recurrence. The cap mutation rejects
before any CuPy import.

The first focused control invocation had eleven passing tests and one harness
failure: a source-text assertion prohibited the phrase `full_width_value`,
but that phrase is required inside the frozen prospective evidence-stage and
claims-boundary strings. The assertion was narrowed to AST import and call-
site authority; no source algorithm, shape, lifetime, cap, tolerance,
population, exact result, or claim changed. The corrected suite adds the
fresh-process import/cap-order control and passes all thirteen tests.

This is a source-seal correction before any device owner or one-shot value,
not a retry, a post-outcome gate relaxation, a numerical tolerance change, or
a favorable scientific substitution.

## Verification scope

All thirteen focused controls pass in 24.687 seconds. A separate 100-test
source-only compatibility group passes in 35.862 seconds. It covers the legal
river bridge, occupied-card quotient, full-width source arithmetic,
literal-45 liveness arithmetic, structured showdown automaton, factorized and
full-width beliefs, exact no-limit betting and exhaustive settlement, and the
action-clock legal spine. No device test or literal owner was invoked in this
checkpoint. This is a compatibility spot-check, not certification beyond the
frozen source boundary and bounded exact population.

## Claims boundary

ADR-0388 establishes that one proposed actual-context consumer schedule is
complete under its named numeric rows, passes fixed source arithmetic, and is
algebraically lossless on the bounded exact control. It establishes no live
allocator admission, device execution, 45-card quotient value, resolver
iteration, solve, legal action, 15-second decision, marginal chip-valued
decision quality per millisecond, general odd-chip or side-pot leaf,
truncation authority, blueprint, complete bot, or poker strength. No
counterpart beyond ADR-0386's one flat-pot river context was sought; that is an
absence claim.
