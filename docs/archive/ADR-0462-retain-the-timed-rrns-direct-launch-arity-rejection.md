# ADR-0462: Retain the timed RRNS direct launch-arity rejection

- Status: accepted retained sole ADR-0461 calibration rejection; the fresh absolute-Git owner compiled and loaded the exact 28-kernel cubin, passed resource and reduced-memory admission, completed and independently verified one positional warmup cell, then the next RRNS timed cell reached `direct_prices_rrns_batch` with eight host arguments for a nine-parameter CUDA declaration and terminated with `CUDA_ERROR_INVALID_VALUE`, leaving zero measured calls and no materiality, topology, arithmetic-schedule, target, resolver, action-clock, quality, truncation, blueprint, or strength result
- Date: 2026-08-26
- Follows: ADR-0461
- Source-seal commit: `08bb6857f47f9669b8f531c65079d4decd52a573`
- Sole command: `.venv\Scripts\python.exe -B run_legal_river_quotient_compiled_global_separation_calibration_v2.py`
- Process exit code: `1`
- Result: `artifacts/work_preflight/legal_river_quotient_compiled_global_separation_calibration_v2.jsonl`
- Result bytes / records / raw SHA-256: `3,299,268 / 23 / 67ac14d408fe8c4299ee603ec1d8c454975094507d4ac28cda73001a42feb90d`
- Independent terminal / events / scientific calls / measured calls: `compiled_reduced_calibration_rejected / 21 / 1 / 0`
- Public elapsed: `173,015,658,900 ns`
- Cubin bytes / SHA-256 / kernels: `582,880 / 2ee4c01232a68ae7608c3f6d1976335621dee61453f7f3b8f432fcde19c10f5b / 28`
- Compile/resource gate: `passed`; frozen spill-load and spill-store ceilings both remain zero
- Device: `NVIDIA GeForce RTX 5080 / compute capability 120 / 17,094,475,776 bytes`
- Reduced campaign peak / physical RRNS arenas / channel capacity: `194,402,210 bytes / 1 / 5`; admission passed
- Completed cell: `10|contract_first_direct_57_scan|positive_witness|opaque_per_source_base|exact_provenance_hit|positional`
- Completed-cell primitive / authority: `2,126,968,200 ns / unbounded integer differential matched`
- Next frozen cell: `12|contract_first_direct_57_scan|positive_witness|lattice_plus_exact_sparse_master_support_correction|cold_base_refresh|batched_five_then_four_RRNS`
- CUDA declaration / timed launch arguments: `9 / 8`; omitted parameter: `source_count`
- Complete-differential launch arguments: `9`; its source count remains present
- Candidate / topology / arithmetic schedule selected: `null / null / null`
- Front-Door-Kind: controller-v1
- Front-Door-Research: ADR-0280
- Front-Door-Process: ADR-0462
- Front-Door-Contract: ADR-0307
- Front-Door-Revoked: ADR-0281
- Front-Door-Active-Next: Preserve the exact consumed v2 journal and all parent source; preregister before source one fresh launch-ABI successor that inserts the missing `np.uint64(scan_count)` argument, derives a complete kernel-arity manifest from the literal CUDA declarations, rejects every arity mismatch centrally before driver dispatch, independently checks every host launch site, retains the exact experiment and absolute-Git lifecycle, and keeps compilation and device work closed until its own clean committed source seal
- Front-Door-Blockers: ADR-0461's owner and v2 result identity are permanently consumed; no complete compiled calibration, measured pass, material-zeta result, production source-local base producer, production exponent admission, selected closure topology, population-25 result, literal-45 numerical result, global resolver-certificate integration, known certificate count per action, or 15-second action result exists; no repeated-actor multiway existence result, off-tree opponent-action result, cross-street belief and certificate handoff, certified full-width river strategy bridge, trained blueprint, integrated bot, production action width, or poker-strength result exists

## Question

What did the sole ADR-0461 invocation establish, what exactly caused its first
terminal, and may that owner be repaired and retried?

## Decision

Retain the 3,299,268-byte journal as the permanent first and only v2 outcome.
The independently constant-bound ADR-0460 reader accepts all 23 records and
returns a complete rejection with 21 events, one scientific call, zero
measured calls, and every selection field null. The additive artifact-bound
assessor binds the raw SHA-256, event inventory, compiled cubin, device and
memory admission, completed cell, terminal, and closed claims. Any byte
mutation, truncation, or extension rejects.

This is a host-to-device launch-arity defect, not an arithmetic or capacity
result. The literal CUDA declaration of `direct_prices_rrns_batch` has nine
parameters:

```text
h, base, prices, cards, source_count, moduli, channels, channel_count, status
```

The timed-cell branch supplied eight and moved directly from `cards` to
`moduli`. The complete-positive differential branch for the same kernel
supplied all nine, including `np.uint64(prepared.source_rows)`. The frozen
SHA-ordered schedule completed the first positional cell and then entered a
12-card batched-RRNS direct cell, where the CUDA driver rejected the malformed
launch before a record for that cell could be committed. This diagnosis follows
from the retained stack, the frozen schedule, and two static call sites; it
requires no device replay.

The run did establish several bounded systems facts. NVCC returned zero, the
582,880-byte cubin was durably captured, compile-resource evidence passed, the
RTX 5080 loaded all 28 functions, and the single five-channel arena passed the
194,402,210-byte reduced campaign admission. The first positional warmup cell
returned witness price one and matched the unbounded-integer authority. Those
facts do not compare topology arms: the call was warmup-only, no retained
repeat ran, and the RRNS schedule never completed one cell.

Do not rerun v2. A successor needs fresh protocol, campaign, scientific-module,
runner, reader, controls, launcher, and result identities. Fixing only this
tuple would leave the defect family representable. The structural successor
must derive kernel parameter counts from the literal CUDA declarations, route
every dispatch through one arity guard, and independently audit all launch
sites before compilation. This scope retires argument-count omissions; it does
not claim that equal-arity semantic misordering is impossible.

## Failure-class boundary

The shared durable owner reduced the rejected-invocation tax materially. The
earlier relative-Git failure left no journal; this failure retained tool
versions, compilation, cubin, resource evidence, device identity, memory
admission, prepared-domain receipts, one exact warmup cell, and a typed outer
terminal. It did not prevent a launch-plumbing defect, but it made the defect
bounded, attributable, and non-replayable.

## Kill criteria

Kill any interpretation that retries or edits v2, calls the rejection an RRNS
arithmetic failure, uses the first positional warmup as arm-comparison evidence,
promotes reduced memory admission to literal-45 fit, treats compile success as
a quality prior, selects a topology, or infers action latency or decision
quality. Kill any successor that changes the frozen CUDA bytes, experiment
matrix, fixtures, schedules, liveness, phases, work coordinates, projection,
thresholds, or claims to obtain a result.

## Claims boundary

ADR-0462 proves only the exact retained lifecycle and the launch-arity diagnosis
above. It supplies no material-zeta speed verdict, selected production
topology, literal-45 numerical evidence, resolver iteration, action-clock fit,
decision-quality result, truncation authority, blueprint result, or poker
strength.

## Consequences

- The v2 command and artifact identity are permanently consumed.
- Compilation, resource admission, reduced allocation, and one positional
  warmup survive as bounded systems evidence.
- Every comparative and production claim remains closed.
- A fresh source-sealed arity-contract successor is eligible only after a
  prospective ADR.
