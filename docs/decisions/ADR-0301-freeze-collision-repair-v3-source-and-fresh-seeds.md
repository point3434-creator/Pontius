# ADR-0301: Freeze collision-repair v3 source and fresh seeds

- Status: accepted source and seed freeze; no fresh panel or sizing value has been constructed
- Date: 2026-08-23
- Follows: ADR-0300
- Front-Door-Kind: controller-v1
- Front-Door-Research: ADR-0280
- Front-Door-Process: ADR-0301
- Front-Door-Contract: ADR-0282
- Front-Door-Revoked: ADR-0281
- Front-Door-Active-Next: Implement the value-free fresh width-four structural generator, construct exactly the frozen 48-context representative family and separate 96-context qualified pool, and commit their identities before opening any full/narrow qualification value; keep collision-repair v3 values and every replay, blueprint, convex-master, resolver, and strategy integration closed
- Front-Door-Blockers: neither fresh structure exists, the candidate-blind qualified runner has not opened a value, no 24-context qualified panel or disjointness digest exists, no v3 value exists, and integration remains unauthorized

## Verdict

Accept ADR-0300's exact-legal source gate and freeze its canonical source
identity plus the two fresh stream identities. The source implements only the
preregistered state-adaptive final origin: retain two pots when its clipped
raise-to is distinct from every mandatory anchor, otherwise substitute three-
halves pot. It retains the fixed one-quarter, one-half, and one-pot origins,
all exact legal non-raises, the three mandatory raise anchors, and exact
adjacent barycentric projection.

This decision opens no card context, full or narrow sizing value, v3 value,
policy, or integration path.

## Frozen source identity

The immutable source id is exactly:

```text
adr-0300-collision-repair-v3
```

Its canonical source SHA-256 is:

```text
ebae17f69c4f37377edf0fb0c55a99049c8688c8517dcbc525230d8e418a811a
```

The canonical ASCII JSON binds algorithm version
`exact-legal-overbet-collision-repair-v3`, core fractions
`(1/4, 1/2, 1)`, primary fraction `2`, collision fallback `3/2`, all three
mandatory anchors, pot-after-call semantics, ties-up integer rounding, exact
clipping/deduplication, the seven-raise/nine-action ceilings, adjacent exact
barycentric projection, and the v2 action-set-superset relation. The source
accepts only an exact betting state and its current exact legal decision; it
has no card, range, payoff, solver, or learned input.

The baseline named by the fresh streams is ADR-0300's already committed
preregistration boundary:

```text
c6f5f676354f3bc08ede23820746d978bbb97f15
```

Using that earlier commit rather than this not-yet-created commit preserves
the temporal ordering. The source digest independently binds the implemented
mechanism.

## Source-gate evidence

Focused controls reproduce both branches, high clipping and complete
deduplication, unequal maximum-contestable and own-all-in anchors, cumulative
short-all-in reopening, all-in-only and closed raising, exact off-tree
interpolation, stale or forged input rejection, wrong-branch provenance
rejection, immutable source state, deterministic canonical bytes, and the
frozen source digest.

The maintained exhaustive traversal covers all 45,456 reachable semantic
six-player three-chip states across every button, including 22,920 active
decisions and 60,732 exact action or street-transition edges. At every active
decision it validates the exact kernel, v3 legality and ordering, at most seven
raises and nine total actions, projection of every exact legal action, and
literal inclusion of ADR-0293 v2's exact action set in v3's set. The parked v2
source digest remains
`c09e69eeb1020c6ec33597a51f11f503696737e393139024c9c5b70e51f07153`,
and its frozen outcome/panel preservation tests pass.

Changed-file Ruff and Python 3.11 grammar parsing pass. The complete repository
suite passes 1,139 tests with two expected skips. Static import inventory finds
the collision-repair source only in its exact-legality module and tests; no
reference replay, blueprint, convex master, resolver, or strategy module
imports it.

## Frozen fresh streams

The representative seed text is exactly:

```text
pontius|adr-0301|collision-repair-v3-representative-v1|baseline=c6f5f676354f3bc08ede23820746d978bbb97f15|source=ebae17f69c4f37377edf0fb0c55a99049c8688c8517dcbc525230d8e418a811a
```

The qualified-pool seed text is exactly:

```text
pontius|adr-0301|collision-repair-v3-qualified-v1|baseline=c6f5f676354f3bc08ede23820746d978bbb97f15|source=ebae17f69c4f37377edf0fb0c55a99049c8688c8517dcbc525230d8e418a811a
```

Encode both as ASCII and use ADR-0293's frozen SHA-256 counter stream,
unbiased `randbelow`, and Fisher-Yates implementation. The two new generator
versions are exactly
`collision-repair-v3-representative-width4-v1` and
`collision-repair-v3-qualified-width4-pool-v1`. Both reuse ADR-0297's width-
four deal layout, pot/stack sets, exact positive 4-by-4 weights, two-chip
minimum bet, and `width4-both-signs-three-rows-three-columns-v1` structural
filter without importing a sizing oracle or the v3 source.

The representative stream accepts the first 48 structurally admissible
contexts and stops. The qualified stream accepts the first 96 structurally
admissible contexts and stops. Structural construction must reject a semantic
duplicate within or across the two fresh families and must establish no
semantic counterpart among ADR-0291 through ADR-0299 contexts. Here,
"no counterpart" is an exhaustively checked absence claim over those finite
maintained context inventories, not a population claim.

Commit the two value-free structures, attempt counts, canonical identities,
and disjointness evidence before any full/narrow qualification call. Only a
later candidate-blind owned runner may open that separate 96-context pool in
order under ADR-0300's frozen floor, ambiguity, numerical, pivot, teacher, and
immediate-stop rules. V3 must remain absent until both final panel identities
are committed.

## Decision

Proceed to value-free structural construction. Do not change the source,
source id, digest semantics, seed text, generator version, inherited structural
rules, context counts, or temporal boundaries after seeing a structure or
value. Any drift rejects this invocation rather than creating an implicit v3
variant.

## Evidence classification

- **Known:** source code shape, canonical source bytes and digest, exact seeds,
  generator versions, import boundary, and test inventory.
- **Reproduced:** focused exact-legality controls, exhaustive reachable-state
  inclusion/projection controls, v2 identity, formatting, grammar, and full
  regression suite.
- **Unopened:** both fresh structures, every fresh full/narrow value, every v3
  value, and all candidate integration.
- **Hypothesis:** the frozen streams will produce disjoint structures and a
  powered qualified panel under ADR-0300 without ambiguity or excessive work.

## Claims boundary

This result establishes only a bounded immutable exact-legal candidate source
and prospective evaluation-stream identities. It does not establish sizing
quality, representative poker frequency, production range width, strategically
exact translation, runtime decision quality, optimized latency, replay or
convex-master compatibility, a trained blueprint, resolving, multiplayer
safety, NashConv, AIVAT, league strength, C5 completion, or a complete bot. No
revoked experiment, external publication, or thesis change is authorized.
