# ADR-0342: Source-seal the width-three transfer-confirmation owner

- Status: accepted source-only width-three transfer-confirmation owner with exact prior-evidence reuse and failure-complete journal; every real confirmation value remains unopened
- Date: 2026-08-24
- Follows: ADR-0341
- Confirmation source canonical-LF SHA-256: `ec24d1710598003e7e76ff9b1062829107e28ba1766e680bec2a1411b736e202`
- Confirmation seal canonical-LF SHA-256: `06ba1396e3ce826249188d0279266907e716188e0bef07f88cea0fe8de39336c`
- Focused-test canonical-LF SHA-256: `35a85de35d8cde7e6ab8504ad0bcb024b5bf2b850697a63874c18c0a7808add4`
- Confirmation protocol SHA-256: `0f1cc9b42a4d4c4a394acca073dee64dcd245f6496d89a3bf708dbdfecad4fa9`
- Confirmation schedule SHA-256: `53389f770aa3fa0773d003df11a48858a1cb42ee166cb44bba74dda42805d7fe`
- Transfer qualified panel SHA-256: `dd46e2917757ae0ba02e620f6298be8eac7629e92db573cea291bf1160b157bf`
- Prospective candidate calls: `126`
- Retained prior arms reused without invocation: `32`
- Prospective artifact: `experiments/results/fresh-action-width-transfer-confirmation-v1.jsonl`
- Synthetic confirmed journal SHA-256: `6c16411c7d596da7969bd92d7fa281fd7977f6250ccc30cc5fd250b96556e77c`
- Synthetic rejected journal SHA-256: `4fbd19a5a44e0e92327c193cb4634a02cc725f61ece8767e1b0c3a6733412c71`
- Front-Door-Kind: controller-v1
- Front-Door-Research: ADR-0280
- Front-Door-Process: ADR-0342
- Front-Door-Contract: ADR-0307
- Front-Door-Revoked: ADR-0281
- Front-Door-Active-Next: From a clean commit containing ADR-0342 and an absent prospective confirmation artifact, invoke `run_and_retain_adr0341_transfer_confirmation` exactly once; retain and fsync every candidate arm before continuation; stop without retry on confirmed, all-conjunct rejection, typed consumer rejection, numerical rejection, unexpected exception, or infrastructure failure; preserve the exact bytes and then source-seal a solver-free real result before any post-transfer interpretation; value-free responder-raise, full-width capacity, blueprint-kernel, and v0a interface work may proceed independently, but no blueprint training is authorized
- Front-Door-Blockers: no width-three transfer result, responder-raise closure, repeated-actor multiway existence result, cross-street belief/certificate handoff, full-width capacity result, certified full-width river strategy bridge, trained blueprint, v0a/v0b integrated bot, off-tree opponent-action result, preparation-bank filling result, frozen evaluation opponent pool, complete 15-second decision, production action width, or poker-strength result exists

## Decision

Accept and source-seal
`pontius.fresh_action_width_transfer_confirmation` as the only prospective
owner of the frozen width-three transfer test. It binds ADR-0338's selected
development raise width and lower-endpoint/smaller-raise mechanism to
ADR-0341's exact 16-context target panel. It cannot select width four, replace
the context-local mechanism with one fixed ladder, soften a gate, add an
abstention policy, or enter the Legal Decision Spine.

The 16 retained complete-integer-universe and 16 retained anchored-width-two
arms are prior evidence. They are rebound through ADR-0341 and referenced by
their exact task, evidence, request, legal-set, LP, journal, terminal, panel,
and campaign identities. They are not invoked again and are not counted as
confirmation calls. For each target context, the prospective owner evaluates
exactly every legal width-three set consisting of the retained minimum and
maximum anchors plus one interior raise-to total. The legal-universe widths
produce `9, 7, 9, 7, 7, 7, 7, 9, 9, 9, 9, 7, 7, 9, 9, 5` candidate calls,
or 126 in total.

Those same 126 values serve two roles without opening another arm. The frozen
direct mechanism selects the greatest certified behavioral lower endpoint,
with an exact tie resolved by the smaller nominal raise-to total. The complete
candidate set also forms the exhaustive width-three interval teacher. Every
certified nondominated candidate survives; the reporting-only equivalence
allowance cannot select or eliminate a candidate.

Unrestricted transfer is confirmed only when all five ADR-0338 conjuncts pass
unchanged on the exact panel:

1. maximum normalized full-regret upper at most `0.005`;
2. mean normalized full-regret upper at most `0.001`;
3. conservative aggregate-recovery lower at least `0.90`;
4. maximum normalized teacher-excess upper at most `0.001`;
5. mean normalized teacher-excess upper at most `0.0002`.

Chip endpoints are summed before aggregate-recovery division. Every context
normalizes through its explicit `payoff_span_chips`; stack is not a permitted
substitute. A completed mean-only, 14-of-16, three-of-five-conjunct, or any
other partial passage is the typed `completed_rejected` result, not a process
failure and not unrestricted confirmation. Any abstention successor requires
a new preregistration and untouched evidence.

No real consumer or solver call occurred. The prospective artifact is absent
and already has a path-specific `.gitattributes` `-text` rule. The module has
one dormant consumer call site behind the uninvoked no-clobber wrapper. It
does not import or call the closed qualification, exhaustive-teacher, or
direct-mechanism runners and emits no betting action.

The inherited trust chain remains explicit. ADR-0310 made native-simplex
robustness the next systems question. ADR-0311's directive is Preregister the
native-simplex robustness audit. ADR-0312's directive is Seal the native-simplex audit compiler and corpora.
ADR-0313's directive is Seal the native-simplex audit runner before results.
ADR-0314's decision is Retain the native-simplex audit and reject the frozen gate.
ADR-0315's directive is Source-seal the artifact-only native-simplex gate correction.
ADR-0316's decision is Accept the corrected audit and bound replacement eligibility.
ADR-0317's directive is Separate solver classes and prioritize the certified sizing adapter.
ADR-0318 binds HiGHS 1.12.0, ADR-0319 requires one public HiGHS-DS call per
canonical task, and All 177 ordered observations pass under ADR-0320, making
the separate consumer eligible. ADR-0321 preserves caller-owned legal fallback,
ADR-0322 returns research evidence or rejection with no action, ADR-0324
remains value-unopened, ADR-0325 was authorized exactly once, ADR-0326 and
ADR-0327 govern the exhaustive bounded development-teacher chain, and ADR-0333
records that No replacement sizing value was opened at its source boundary.
ADR-0330 remains permanently closed, ADR-0331's append-and-fsync discipline
and ADR-0332's exclusive `xb` open remain authoritative, ADR-0337 remains the
response-closed direct mechanism with a dynamic-branch rebinder, ADR-0338 alone
records the selected development raise width, and ADR-0339's exact non-overlap
comparison remains a finite absence claim, not representativeness evidence.
ADR-0340's 192 prospective tasks remain distinct from ADR-0341's 94 accepted
one-call arms and from this owner's 126 prospective candidates. ADR-0341
qualifies the exact panel but supplies no confirmation value. No result here
weakens those authorities.

## Exact schedule and evidence chain

The schedule SHA-256
`53389f770aa3fa0773d003df11a48858a1cb42ee166cb44bba74dda42805d7fe`
binds:

- ADR-0338 retained result
  `7317ff19c02efe9fa084802120289286c2a6eb1885b816087ba58b710ca27353`;
- ADR-0341 target panel
  `dd46e2917757ae0ba02e620f6298be8eac7629e92db573cea291bf1160b157bf`;
- 16 retained full comparators and 16 exact width-two incumbents;
- 16 baseline `GreedySubsetTask` identities wrapping the exact retained
  width-two requests;
- 126 width-three candidate tasks, 126 complete own-block plus fold/call
  response-closure transitions, and 126 ordered call slots.

The task inventory is 142 because prior full comparators are references rather
than greedy tasks. This is not a 142-call schedule. The call ledger is exactly
126 prospective arms; the 32 earlier qualification arms remain separately
reported prior work. Neither count is a complete poker decision or a latency
measurement.

Every accepted real arm must retain the exact request, public state,
kernel-legal raise set, compact LP, policy, responder best actions, raw dual
hints, behavioral lower endpoint, certified upper endpoint, and one-public-
call count. The solver-free rebinder reconstructs the feasible behavioral
value and independent dual certificate before it admits an endpoint. It then
reconstructs every finite-block price, greedy selection, exhaustive teacher,
full regret, teacher excess, payoff-span normalization, aggregate recovery,
and gate Boolean.

## Durable terminal contract

The owner exclusively creates the prospective path with `xb`. A candidate may
start only from the preceding post-write, post-flush, post-`fsync` receipt.
Every accepted, rejected, or unexpected arm becomes a durable observation
before semantic reduction or continuation. There is no overwrite, resume,
alternate backend, skipped candidate, or retry authority.

The five semantic terminal reasons are distinct:

- `completed_confirmed`;
- `completed_rejected`;
- `consumer_rejected`;
- `numerical_rejected` with candidate-price, context-reduction, or campaign-
  reduction stage;
- `unexpected_exception` with incomplete invocation accounting.

Infrastructure failures retain their exact phase, raw bytes, last receipted
semantic prefix, known call count, whether an invocation may be unreceipted,
and recovery failure if the raw path cannot be read or parsed. A complete
first record from the wrong protocol or synthetic/real campaign is rejected
before generic torn-prefix recovery; it cannot masquerade as an empty prefix.

The completed synthetic controls contain 128 records each: one header, 126
observations, and one terminal. The confirmed control passes all five
conjuncts; the rejected control completes the same schedule but fails maximum
and mean full regret plus aggregate recovery. Their values are fake systems
fixtures and cannot become research evidence. Rehashed semantic corruption,
wrong-mode rebinding, torn tails, typed rejection, unexpected failure,
numerical reversal, observation-append failure, no-clobber launch, and fake
real witness attacks all fail closed in the focused tests.

## Claim boundary

This checkpoint proves prospective ownership, schedule identity, evidence
reuse, failure survival, and solver-free reduction only. It supplies no real
width-three transfer result, elapsed time, production action width, responder-
raise or multiway response closure, full-width capacity, blueprint, complete
decision, or poker-strength evidence. Passing synthetic endpoints are not a
quality prior. ADR-0341 remains qualification evidence only, and ADR-0338
remains development evidence only, until the separately retained real
confirmation says otherwise.

## Next gate

Commit this exact source boundary while the prospective artifact is absent.
From that clean commit, call
`run_and_retain_adr0341_transfer_confirmation` once. Preserve whatever exact
semantic or infrastructure terminal occurs, with no retry. A later read-only
result owner may publish unrestricted width-three transfer only from an exact
`completed_confirmed` journal whose every witness and all five conjuncts
rebind. No other result authorizes width reselection, partial success,
blueprint training, or production integration.
