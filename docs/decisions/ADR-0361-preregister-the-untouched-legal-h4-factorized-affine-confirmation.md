# ADR-0361: Preregister the untouched legal h4 factorized-affine confirmation

- Status: accepted source-only preregistration after ADR-0360; the exclusive 32-section owner, frozen downstream direction compiler, reconstruction-complete evidence serializer, exact provenance snapshot, laboratory walls, and first-terminal retention are sealed while every fresh direction endpoint, factorized section, target timing, natural mode, cardinality, action, quality row, and strategy label remains unopened
- Date: 2026-08-25
- Follows: ADR-0360
- Config: `experiments/configs/legal-h4-factorized-affine-confirmation-v1.json`
- Config SHA-256: `07047abf345d5278023cc6820d4ce196249bf8aa8668a0457a871af50169e638`
- Parent decision canonical-LF SHA-256: `ddb8b204053d2d424275798ccdea9b09ab8ace607494283c1814b0c074520e80`
- Parent protocol SHA-256: `c75ef3f8eb5011775049080b98302fec0d3f6c72bf6cf58bacfa1146fe0752fa`
- Direction compiler canonical-LF SHA-256: `6d1e2db628cfa698347268c80a6062a8a9367e93bdbd51c34695815366b3f9fa`
- Direction controls canonical-LF SHA-256: `86c49b2476622fc240f8ad802c64fed434f9e8a47152679c3c08cb98d2631f58`
- Complete evidence serializer canonical-LF SHA-256: `27fbf408143ec10e4cc25b76b60bc6eaf42eee5e8a957c616042588faebe20d8`
- Complete evidence controls canonical-LF SHA-256: `feef4e905d79e448ac87670df0bc4851b604661f1d9a08df7bec9c28ca1e3660`
- Prospective runner canonical-LF SHA-256: `65875f914b75ca282f3c785daee71bb1c7ba39909ae1298c1353368739d0b573`
- Runner controls canonical-LF SHA-256: `38913f8cd1aeb541dc52facb7ea587287ae967cb4d5d22f1bf58edda069ff220`
- Prospective result: `experiments/results/legal-h4-factorized-affine-confirmation-v1.json` (absent)
- Front-Door-Kind: controller-v1
- Front-Door-Research: ADR-0280
- Front-Door-Process: ADR-0361
- Front-Door-Contract: ADR-0307
- Front-Door-Revoked: ADR-0281
- Front-Door-Active-Next: From one clean commit containing ADR-0361 and an absent result path, invoke exactly once: `pontius.legal_h4_factorized_affine_confirmation`; retain the first all-conjunct pass, scientific rejection, typed row-growth rejection, exception, infrastructure failure, wall failure, or byte-bound terminal without changing the four ADR-0360 contexts, direction recipes, exact row/master/cap checks, two target players, complete raw schema, typed dispatch, bounds, walls, output path, or claims policy; never replay ADR-0358, and keep literal full-width river capacity independent
- Front-Door-Blockers: no untouched factorized affine confirmation result, repeated-actor multiway existence result, off-tree opponent-action result, cross-street belief and certificate handoff, full-width capacity result, certified full-width river strategy bridge, sealed blueprint trainer/checkpoint/abstraction/slice-audit chain, trained blueprint, v0a/v0b integrated bot, preparation-bank filling result, frozen evaluation opponent pool, complete 15-second decision, production action width, or poker-strength result exists

## Question

Across the exact four value-unopened ADR-0360 legal h4 contexts, do the frozen
three one-step DCFR history vertices and one independently audited row-growth
proposal admit all 32 exact fan-as-ray/factorized-face-as-point sections under
the complete evidence, semantic, work, laboratory-wall, and no-Cartesian-
materialization contract?

This asks for finite mechanism confirmation on four deliberately frozen fresh
h4 contexts. It does not ask whether the representation scales to full river
width, fits the 15-second action clock, closes repeated-actor multiway
responses, transfers across streets, selects a legal action, or improves poker
strength.

## Decision

Add `legal_h4_factorized_affine_confirmation` as the sole exclusive owner of
the ADR-0360 target. Its result path remains absent. The owner binds the exact
source population and every transitive load-bearing implementation through a
strict single-snapshot configuration, refuses dirty Git before target work,
and can create only one exclusive first terminal. No fresh direction endpoint,
best response, selector result, affine section, mode, cardinality, timing,
action, quality row, or strategy label has been opened by this decision.

The inherited front-door trust chain remains explicit. ADR-0310 made native-
simplex robustness the next systems question. ADR-0311's directive is
Preregister the native-simplex robustness audit. ADR-0312's directive is Seal
the native-simplex audit compiler and corpora. ADR-0313's directive is Seal the
native-simplex audit runner before results. ADR-0314's decision is Retain the
native-simplex audit and reject the frozen gate. ADR-0315's directive is
Source-seal the artifact-only native-simplex gate correction. ADR-0316's
decision is Accept the corrected audit and bound replacement eligibility.
ADR-0317's directive is Separate solver classes and prioritize the certified
sizing adapter. ADR-0318 binds HiGHS 1.12.0, ADR-0319 requires one public
HiGHS-DS call per canonical task, and All 177 ordered observations pass under
ADR-0320, making the separate consumer eligible. ADR-0321 preserves
caller-owned legal fallback, ADR-0322 returns research evidence or rejection with no
action, ADR-0324 remains value-unopened, and ADR-0325 was authorized exactly
once. ADR-0326 and ADR-0327 govern the exhaustive bounded development-teacher
chain. ADR-0328 retains that exhaustive teacher and solver-free rebinder before
the direct closed finite-block greedy line. ADR-0330 remains permanently
closed; ADR-0331's append-and-fsync discipline, ADR-0332's exclusive `xb` open,
and ADR-0333's statement that No replacement sizing value was opened remain
authoritative. ADR-0334 and ADR-0335 bind the 2,113-task non-replay chain;
ADR-0336 records width three; ADR-0337 owns the response-closed direct
mechanism; and ADR-0338 alone records the selected development raise width.
ADR-0339's exact comparison remains a finite absence claim. ADR-0340's 192
prospective tasks remain distinct from ADR-0341's 94 accepted one-call arms
and ADR-0343's 126 confirmation arms; ADR-0342 alone authorized that retained
confirmation invocation. ADR-0344 and ADR-0345 own the finite h4 legal
responder-raise keystone line. ADR-0346 and ADR-0347 lead only to responder-row
growth; ADR-0348 and ADR-0349 lead only to selector-window work. ADR-0350
opened selector-stable affine integration as a question; ADR-0351 replaced it
with the tie-aware legal h4 affine-envelope requirement. ADR-0352's owner is
closed by ADR-0353 before any fresh untouched tie-aware affine result. ADR-0354
source-seals the factorized exact active-set directional calculus; ADR-0355's
owner is closed by ADR-0356. ADR-0357 seals the factorized affine consumer.
ADR-0357 requires an exclusive legal h4 owner. ADR-0358 owns its one
same-fixture integration invocation, and ADR-0359 permanently closes it while
requiring a fresh value-unopened confirmation. ADR-0360 alone fixes that
population and authorizes only an exclusive untouched legal h4 owner. No
earlier owner is revived.

## Frozen direction boundary

`legal_h4_factorized_affine_confirmation_directions` opens exactly four
directions per context inside the exclusive terminal:

1. one one-step warm-started DCFR regret vertex for each sorted public history
   `p0:raise-to-2/p1:raise-to-4`,
   `p0:raise-to-3/p1:raise-to-4`, and `root`; and
2. one converged one-seat row-growth proposal using acting player zero, guard
   `0.25`, maximum 128 iterations, and tolerance `1e-10`.

The row-growth endpoint is an untrusted proposal. Before it becomes eligible,
the compiler independently rebinds every response-row constant and coefficient
with exact Fraction arithmetic, every candidate utility, best-response value,
deviation gain and NashConv, the cap construction, candidate feasibility,
convergence classification, response-signature uniqueness, and every master
duality gap and original-coordinate constraint violation. The cap identity is
an explicit semantic gate rather than an incidental Float64 coincidence. The
semantic row-growth digest excludes the three measured per-iteration timing
fields, so identical scientific work retains one identity across invocations.
A failed conjunction raises a typed scientific rejection carrying the complete
non-policy audit; the context is not dropped or replaced.

Development-only controls reproduce the four already-opened historical
endpoint hashes on the old fixture. That proves compiler compatibility, not a
fresh outcome. Adversarial row, master-duality and cap mutations each reject.
The direction module has no import-time target call and imports no closed legal
h4 result owner.

## Reconstruction-complete section evidence

`complete_factorized_affine_evidence` serializes every field ADR-0359 could
only authenticate:

- all fan cells, segments, boundary points, total and reachable tapes, tie
  states, measures, domains, and tape identities;
- every fan-row tape, exact intercept, slope, domain, and independent digest;
- every source and non-source point face with its complete information-set
  factors, parent sequences, legal and maximizing actions, support bit, local
  slope extrema, extremal tapes, exact value algebra, both cardinalities, full
  work ledger, bigint bit lengths, and face identity;
- every compact convex piece and selector-window field; and
- the complete exact residual matrix for every fan row at every sampled point,
  with `z >= row` orientation and active-row counts.

Live checks compare the entire record back to the independent object, not just
to an outer hash, then separately verify complete raw factors, fan coverage,
exact authority and domain, convex piece partition, dual-cardinality semantics,
epigraph completeness and direction, work arithmetic, and zero materialized
tapes. A rehashed compact-piece mutation therefore still fails. The serializer
contains neither a Cartesian tape enumerator nor a result writer.

## Workload, gates, and non-gates

The only target matrix is four contexts by four directions by target players
zero and one: exactly 32 uniquely bound coordinates. Each coordinate performs
one `build_factorized_tie_aware_affine_section` call. Exact total-function
cardinality is certificate authority; reachable-support cardinality is
reporting-only. Exact source ties dispatch to the complete factorized maximum
envelope. Exact singletons alone dispatch to selector-window v2 or its typed
fail-closed result. The fan is ray authority, the two-pass factorized face is
point authority, and the epigraph direction is `z >= row`.

Gates require exact population and context identity, the complete ordered
direction inventory, all row-growth audit conjuncts, all 32 coordinates,
every complete-evidence check, typed dispatch, exact authorities, separate
cardinality meanings, zero materialized tapes, a clean commit, finite output,
zero actions and quality labels, and the frozen walls and result-byte bound.
The config recursively rejects any natural endpoint hash, mode, source tie,
window, fan size, piece count, cardinality, section identity, or target timing
as an expected result or gate. These quantities may appear only as retained
observations after the one-shot call.

One section build may consume at most 60 seconds. `subject_seconds` is the sum
of exactly 32 individually measured live section builds and may consume at
most 360 seconds; it excludes direction generation, evidence construction,
checks and serialization. The 600-second campaign wall covers population
rebinding through complete scientific payload and gate materialization but
excludes config loading, Git inspection, final rendering, `fsync`, and process
overhead. The explicit tree guard is 100,000 nodes, the fan guard is 256
pieces, and the first terminal may contain at most 8,388,608 bytes. None is an
action-clock or full-width capacity claim.

## One-shot retention and controls

The config and prospective result paths are `-text`; line-ending conversion
cannot change their retained byte identity. The owner uses `O_EXCL`, flush and
`fsync`. A malformed config or dirty tree stops before fresh target work. A
scientific failure retains its partial audit, while an exception,
infrastructure failure, wall failure, oversize artifact, or complete pass is
also a final non-replayable terminal. No resume, overwrite, retry, context
drop, reseed, gate repair, or partial-pass abstention exists.

Twenty-one focused controls pass without opening the ADR-0360 target. They
cover complete tie and singleton records, non-source factors, every-row
epigraph direction, rehashed semantic mutations, absence of Cartesian/write
paths, historical endpoint compatibility, exact row/master/cap mutations,
parameter and nominal identity changes, strict transitive hashes, recursive
outcome-gate exclusion, workload call-graph shape, dirty-Git preemption,
typed partial failure, malformed config, and exclusive no-clobber retention.
The corrected complete repository regression passes 1,678 tests with exactly
two intentional skips: the retained rejected ADR-0278 runner and the revoked
ADR-0281 GPU seed path remain visible tombstones and are never invoked.

## Claims boundary and next gate

This decision supplies a reconstruction-complete one-shot instrument, not its
measurement. It says nothing about the natural direction endpoints, source
ties, fan geometry, cardinality, runtime, full-width scaling, action-clock
feasibility, action quality, strategy strength, multiway response closure, or
cross-street handoff.

Commit this complete boundary with the result path absent. From that clean
commit, invoke exactly once:

```powershell
$env:PYTHONPATH = "src"
.\.venv\Scripts\python.exe -B -m pontius.legal_h4_factorized_affine_confirmation
```

Retain the first terminal exactly. Do not alter a population member, direction
recipe, audit gate, selector allowance, raw field, authority, bound, wall,
output path, or claim after inspection. Never invoke ADR-0358 again. Literal
full-width capacity remains an independent label-free lane.
