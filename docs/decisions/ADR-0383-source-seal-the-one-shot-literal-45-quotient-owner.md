# ADR-0383: Source-seal the one-shot literal-45 quotient owner

- Status: accepted source-sealed but uninvoked literal-45 CUDA owner; exact target fixture, named allocation births, independent samples, live admission, streamed validation, ownership telemetry, durable first-terminal journal, CuPy-free rebinding, and permanent failure classes are implemented and controlled while the public result remains absent and every target value, timing, allocation observation, scalable-contraction, solve, action, quality, truncation, and strength claim remains unopened
- Date: 2026-08-25
- Follows: ADR-0382
- Config canonical-LF SHA-256: `8fbff13aa40b36e6286e0142d355e9e039b0816e402ee43aa2e5f71239689c40`
- Target canonical-LF SHA-256: `d684be8805a7a38f80fc7cd525996ce0cb902fee0af7a4b18bf7f519c430ffd8`
- Runner canonical-LF SHA-256: `2660ab0ef2e3fa6a915a764d455b6a38a879869907f6baef86aa45222817963d`
- Reader canonical-LF SHA-256: `93932fc5c397b2afaa1903938519390f0e6de38e82e670df16ad88affc47593b`
- Target controls canonical-LF SHA-256: `69e5a675bd01facbdac7227bdb7d8805fb8ffed303e0384975b7c435f59e04cf`
- Reader controls canonical-LF SHA-256: `719f1562c3672ed21f740a698efbc488456d4e5b7f1a953b53fe3ebf36730420`
- Result: `artifacts/literal_45_quotient_target_v1.jsonl` (absent; never created)
- Front-Door-Kind: controller-v1
- Front-Door-Research: ADR-0280
- Front-Door-Process: ADR-0383
- Front-Door-Contract: ADR-0307
- Front-Door-Revoked: ADR-0281
- Front-Door-Active-Next: After ADR-0383 and its exact source hashes are committed at a clean boundary, invoke exactly once the no-argument repository `.venv` command `python -B -m pontius.literal_45_quotient_target_runner`; accept its first exclusive durable terminal without retry, repair, alternate path, smaller population, allocator/order substitution, private target call, tolerance change, or post-outcome configuration, then perform a separate artifact-only assessment before any capacity, truncation, timing, solve, action, quality, or strength claim
- Front-Door-Blockers: no literal-45 owner invocation, live target admission, allocator high-water, literal scalable full-width contraction, certified truncation mechanism, repeated-actor multiway existence result, off-tree opponent-action result, cross-street belief and certificate handoff, certified full-width river strategy bridge, sealed blueprint trainer/checkpoint/abstraction/slice-audit chain, trained blueprint, v0a/v0b integrated bot, preparation-bank filling result, frozen evaluation opponent pool, complete 15-second decision, production action width, or poker-strength result exists

## Question

Does ADR-0382's prospective contract now have one source-sealed owner whose
first literal-target outcome will be durable before configuration, provenance,
runtime, admission, allocation, or numerical work can fail?

## Decision

Accept the additive config, target, runner, reader, and controls as the sole
literal-45 source boundary. Do not invoke it in this decision. The public
command is exactly:

```powershell
$env:PYTHONPATH = "src;."
& .\.venv\Scripts\python.exe -B -m pontius.literal_45_quotient_target_runner
```

It accepts no arguments and owns only
`artifacts/literal_45_quotient_target_v1.jsonl`. The path has one exact
`-text` rule, is absent at source seal, and is opened with exclusive `xb`.
The owner fsyncs a value-free header before loading the config, reading Git
provenance, importing the target, importing CuPy, compiling the target
fixture, or allocating a target-shaped numeric array. Every later config,
Git, runtime, telemetry, admission, import, allocation, scientific, release,
and protocol failure therefore has a durable place to terminate. An existing
or torn path consumes the authority; there is no partial, repair, rename, or
retry path.

The inherited front-door trust chain remains explicit. ADR-0310 made native-
simplex robustness the next systems question. ADR-0311's directive is
Preregister the native-simplex robustness audit. ADR-0312's directive is Seal
the native-simplex audit compiler and corpora. ADR-0313's directive is Seal
the native-simplex audit runner before results. ADR-0314's decision is Retain
the native-simplex audit and reject the frozen gate. ADR-0315's directive is
Source-seal the artifact-only native-simplex gate correction. ADR-0316's
decision is Accept the corrected audit and bound replacement eligibility.
ADR-0317 separates solver classes; ADR-0318 binds HiGHS 1.12.0; ADR-0319
requires one public HiGHS-DS call per canonical task; and All 177 ordered
observations pass under ADR-0320. ADR-0321 preserves caller-owned legal
fallback, ADR-0322 returns research evidence or rejection with no action,
ADR-0324 remains value-unopened, and ADR-0325 was authorized exactly once.
ADR-0326/0327 govern the exhaustive bounded development teacher; ADR-0328
retains it and the solver-free rebinder; ADR-0330 remains permanently closed;
ADR-0331's append-and-fsync discipline, ADR-0332's exclusive `xb` open, and
ADR-0333's No replacement sizing value was opened statement remain binding.
ADR-0334/0335 bind the 2,113-task non-replay chain; ADR-0336 records width
three; ADR-0337 owns the response-closed direct mechanism; ADR-0338 alone
records the selected development raise width; and ADR-0339 remains a finite
absence claim. ADR-0340's 192 prospective tasks remain distinct from
ADR-0341's 94 accepted one-call arms and ADR-0343's 126 confirmation arms;
ADR-0342 alone authorized the retained confirmation. ADR-0344/0345 own the
finite h4 responder-raise keystone. ADR-0346/0347 lead only to responder-row
growth; ADR-0348/0349 lead only to selector-window work. ADR-0350 opened
selector-stable affine integration; ADR-0351 replaced it with tie-aware legal
h4 affine envelopes; ADR-0352 is closed by ADR-0353; ADR-0354/0355/0356 own
the factorized exact face result; ADR-0357/0358/0359 own and close same-fixture
integration; and ADR-0360/0361/0362 alone own the untouched confirmation and
assessment. ADR-0363 and ADR-0365 remain consumed; ADR-0364 remains exactly
`GetProcessMemoryInfo failed`; ADR-0366 remains exactly
`representation_rejected_before_target_allocation` with zero target calls.
ADR-0367 through ADR-0377 own the bounded quotient algebra and CUDA ladder.
ADR-0378/0379 own only source-level literal liveness. ADR-0380/0381 own only
the bounded validation seam. ADR-0382 preregisters this owner; ADR-0383 seals
its source without invoking it. No earlier owner is revived.

For machine-checked continuity, the canonical historical wording is retained
without compression. ADR-0317's directive is Separate solver classes and
prioritize the certified sizing adapter. All 177 ordered observations pass
under ADR-0320, making the separate consumer eligible. ADR-0326 and ADR-0327
govern the exhaustive bounded development-teacher chain. ADR-0328 retains
that exhaustive teacher and solver-free rebinder before the direct closed
finite-block greedy line. ADR-0334 and ADR-0335 bind the 2,113-task non-replay
chain. ADR-0344 and ADR-0345 own the finite h4 legal responder-raise keystone
line. ADR-0346 and ADR-0347 lead only to responder-row growth. ADR-0348 and
ADR-0349 lead only to selector-window work. ADR-0351 requires the tie-aware
legal h4 affine-envelope. ADR-0352's owner is closed by ADR-0353 before any
fresh untouched tie-aware affine result. ADR-0354 source-seals the factorized
exact active-set directional-face diagnostic; ADR-0355's owner is closed by
ADR-0356, which retains the directional-face diagnostic result before
tie-aware affine integration. ADR-0357 seals the factorized affine consumer and
requires an exclusive legal h4 owner. ADR-0358 owns its one same-fixture
integration invocation, and ADR-0359 permanently closes it while requiring a
fresh value-unopened confirmation. ADR-0380 freezes the complete ordered
populations 10 and 22. ADR-0383 source-seals the literal-45 config and owner;
the active boundary is one clean public invocation after commit.

The required lifecycle markers remain invoke exactly once, exclusive
untouched legal h4, selector-window, 2,113-task, exhaustive bounded
development-teacher, response-closed direct mechanism, and caller-owned legal
fallback.

## Mechanism and lifecycle

The target compiler is distinct from the 10/22-card bounded seam and from the
consumed staged owners. It constructs exactly 45 cards, 990 hands, 8,145,060
six-card occupancies, 148,995 four-card occupancies, 893,970 labeled query
records, one component, rank 127, width 128, 90 source pairings, six query
labels, and the sealed structured affine automaton. The five large hand axes
and controlled-seat singleton use the same deterministic fixture family as
ADR-0379; this is still a contraction fixture, not a calibrated poker belief.

CuPy and both sealed kernel modules load only inside the one target call.
After context initialization and kernel compilation, the owner synchronizes,
collects Python garbage, clears default and pinned pools, and samples typed
Win32 host/process plus CUDA memory. Admission requires the exact RTX 5080,
compute capability 120, 17,094,475,776 device bytes, driver 13030, runtime
13020, and CuPy 14.2.0; zero pool used/total and pinned blocks; zero target
numeric allocations and scientific calls; 9,353,336,216 host bytes plus an
8,000,000,000-byte live reserve; and 11,755,029,796 device bytes plus a
2,000,000,000-byte live reserve. Failure returns
`live_admission_rejected` with one before-allocation row and zero target work.

After admission, 35 ordered named numeric births cover the host fixture and unary
variants, all forward arrays, four complete host validation buffers, both
direct-sample arrays, the three direct-query arrays, query covectors, and the
three adjoint arrays. Any named birth can produce one typed
`allocation_rejected` observation bound to its exact ordinal, only legal
preceding transition, exact partial invocation/scientific-call state, and
blank future evidence. Cleanup must return both pools to zero, and no later
phase may appear.

A complete run has the exact 17 ADR-0381 transitions. One active device unary
is allocated and overwritten twice. Four source builds, six signed queries,
six affine folds, one direct scan, and two adjoints yield 19 named scientific
calls. Complete source and compatible references live only on the host. The
source reference is 8,340,541,440 bytes in exactly 125 increasing 64-MiB
chunks; compatible validation is 915,425,280 bytes in 14 chunks. Forward
state dies and cached blocks are cleared before the unique 8.34-GB adjoint is
born; only the query covector and cardinality offsets bridge that seam.

Every transition records exact ownership, default-pool used/total, pinned
blocks, CUDA free/total, host available physical bytes, process working set,
process private bytes, and the frozen phase limit. Final release requires
zero pools/pinned blocks, stable device total, device-free recovery within
16 MiB, process-private recovery within 256 MiB, and host-available recovery
within 512 MiB. Release and the 600,000-ms laboratory guard are infrastructure
gates, never numerical, action-clock, or decision-quality gates.

## Independent validation and reader

Source sample ranks are
`0,8145059,5342038,4228481,2201283,4711753,6939721,5807274,2769721,2288776,7476024,272683,1670744,2715388,5176110,6960326`.
Query ranks are
`0,148994,23812,83359,148110,22497,10679,116159`; features are
`0,1,2,31,63,95,126,127`. The target source independently regenerates them
from `pontius|adr-0382|target=45`, endpoints, SHA-256 labels, and deterministic
collision repair before comparing the frozen tuples.

Warm, refreshed, query-only, and adjoint repetitions compare literal bytes.
The direct scan consumes neither the recurrence nor a device reference copy.
Forward and transpose dots copy the device operand through the frozen host
window, use NumPy Float64 `dot` per chunk, and combine in increasing order
with `math.fsum`. SHA-256 remains reporting-only. Error observations are
canonical finite hexadecimal Float64, must be nonnegative, and retain the
unchanged `2e-12`, `2e-8/2e-11`, `2e-10`, and `2e-8/1e-10` ceilings. No
complete-ten-card gate exists at the target.

The reader imports only the standard library and the standard-library durable
journal. It independently reconstructs geometry, work, source/compatible
chunk covers, admission, runtime identity, all ownership transitions, phase
limits, invocation counts, allocation-birth/transition legality, nonnegative
numerical ceilings, operand roles, wall classification, and release from raw
fields. It binds the exact campaign identity and refuses a stored terminal
that differs from the reconstructed class. Digests cannot decide a gate.

The terminal is exactly one of `completed_pass`,
`live_admission_rejected`, `allocation_rejected`, `scientific_rejected`, or
`infrastructure_failure`. Every one is permanent.

## Controls and pre-seal corrections

Thirteen source-only tests pass in 0.780 seconds. They cover exact geometry,
work, 35/19 call ledgers, target sample regeneration, 125/14 complete chunk
covers, canonical config/dependency hashes, result absence, the exact
attribute rule, fresh `-B` imports with CuPy absent and all counters zero, AST
absence of consumed owners and target calls, lazy target import after the
header, all five synthetic terminal classes, header durability before every
injected dependency, exclusive replay rejection, pre-observation exceptions,
oversized observations and exception messages, torn journals, a completely
rehashed semantic mutation, digest non-authority, negative-error rejection,
and allocation-birth/transition mismatch.

The 118-test quotient, liveness, bounded-CUDA, journal, runner, memory-
telemetry, and full-width arithmetic dependency family passes in 18.937
seconds after the ordinal/partial-work repair.

Final whole-repository discovery ran 1,831 tests in 909.737 seconds: 1,828
passed, two historical optional tests skipped, and the sole failure was the
preserved immutable ADR-0365 assertion that the subsequently retained
ADR-0366 v2 result must be absent. Thus this source seal introduced no new
repository regression; this is a compatibility spot-check, not certification
of the uninvoked literal target.

Four controls materially improved the prospective source before sealing.
First, the direct source sample originally used advanced indexing without a
named allocation wrapper. It now allocates explicit rank and sample-workspace
births, and the complete allocation ledger is frozen at 35. Second, numerical
ceilings originally checked only `error <= limit`; the independent reader now
requires `0 <= error <= limit`, so a negative semantic impostor cannot pass.
Third, a crossed 600-second laboratory wall could have fallen through to
`scientific_rejected`; wall or release rejection now maps only to
`infrastructure_failure`. Fourth, the synthetic allocation-rejection path
could pair a later birth with the first allocation count and carry complete
future invocation fields. The reader now binds all 35 births to their exact
ordinal, predecessor, partial invocation/scientific count, and blank future
fields; the direct-scan counter increments only after its three device
allocations succeed. No target import, CuPy import, target fixture,
target allocation, target value, or result path was opened while making
these corrections.

## Invocation and kill rule

The source seal authorizes one future call only after this exact boundary is
committed and clean. The first durable terminal is accepted as-is. Kill and
close on any config/provenance/runtime/import/telemetry/admission/allocation/
numerical/release/wall/artifact failure, a missing raw field, a second path,
post-terminal continuation, an unrecognized allocation birth, an unpriced
temporary, target access from tests, ambient pool allowance, lower-width
substitution, retry, or source/hash drift. Do not modify this owner after a
result exists; assess the retained bytes in an additive ADR and reader.

## Claims boundary

ADR-0383 establishes source readiness and synthetic lifecycle behavior only.
It establishes no live admission, literal allocation, target high-water,
target numerical passage, target timing, throughput, scalable full-width
contraction, solver iteration, solve, legal action, 15-second decision,
decision quality, truncation authority, or poker strength. The real result is
absent. "No counterpart found" remains an absence claim, and no systems
result may be used as a quality prior.
