# ADR-0403: Retain the work-preflight v3 resource-inspector rejection

- Status: accepted retained first-invocation compiler/resource-instrument rejection; ADR-0402's serializer-safe v3 owner is permanently closed after clean provenance, literal bootstrap, and the real no-CUDA serializer probe passed, then the actual CUDA 13.3 `cuobjdump --dump-resource-usage` command returned exit status 4294967295 on the temporary ELF cubin before any raw resource row, calibration population, phase row, projection, complete 25-card numerical value, actual 45-card value, action, quality, truncation, blueprint, or strength result
- Date: 2026-08-26
- Follows: ADR-0402
- Invoked source commit: `ee42b8d60efb7f7334a4c7beff1a295cf1d19795`
- Command: `python -B -m pontius.legal_river_quotient_cuda_compensated_work_preflight_v3_runner`
- Terminal: `compiler_or_primitive_rejection`
- Exact retained reason: `CalledProcessError: Command '['C:\\Program Files\\NVIDIA GPU Computing Toolkit\\CUDA\\v13.3\\bin\\cuobjdump.exe', '--dump-resource-usage', 'C:\\Users\\point\\AppData\\Local\\Temp\\tmp3fju199y.cubin']' returned non-zero exit status 4294967295.`
- Result: `artifacts/work_preflight/legal_river_quotient_cuda_compensated_work_preflight_v3.jsonl`
- Result bytes: `15783`
- Result SHA-256: `b84d9cd22042427c88f9c42b2da7acd176cdd0d5dec654c7f361bae7fa79cd0d`
- Journal records: `7` (`header`, `provenance`, `bootstrap_handshake`, `serializer_probe`, `laboratory`, `terminal_evidence`, `terminal`)
- Independent reader: `compiler_or_primitive_rejection`, `passed=False`, `event_count=5`, `handshake=True`, `serializer_probe=True`, `phase_count=0`, `projection=None`
- Actual runtime: `NVIDIA GeForce RTX 5080`, compute capability `120`, device bytes `17094475776`, driver `13030`, runtime `13020`, CuPy `14.2.0`
- Immutable v1 result: SHA-256 `fd8c71ddb534320577dfc9a390946fc3dffe3fe806bf93d456ac33e55fe8e830`, unchanged
- Immutable v2 result: SHA-256 `9b9a3f606004a281773f6dc83c86fe2f70811ab825fbf1e0b8b47c1c75abdad3`, unchanged
- Reserved actual result: `artifacts/legal_river_quotient_cuda_consumer_v1.jsonl` absent
- Front-Door-Kind: controller-v1
- Front-Door-Research: ADR-0280
- Front-Door-Process: ADR-0403
- Front-Door-Contract: ADR-0307
- Front-Door-Revoked: ADR-0281
- Front-Door-Active-Next: Preserve the immutable seven-record v3 terminal and all three closed work-preflight owner identities, then preregister a new diagnostic-only exact-cubin inspector qualification before any capacity successor: use new config/protocol/campaign/result identities; compile only the unchanged hash-bound CUDA source with the unchanged five caller options; retain the exact ELF cubin bytes or a lossless bounded encoding plus SHA-256; invoke candidate CUDA 13.3 inspectors with `check=False`; durably retain command, return code, stdout, stderr, and tool identity; require exact rows for all three direct kernels and conservative agreement with the independent driver attributes before selecting an inspector; preserve every population, arithmetic, phase, ratio, resource ceiling, wall, stop, and claims boundary; do not invoke v1, v2, or v3, run calibration, construct population 25 numerically, publish externally, or infer capacity, latency, action quality, truncation, blueprint, or poker strength
- Front-Door-Blockers: no qualified exact-cubin resource inspector, retained 10/22-card calibration, conservative complete-25 capacity verdict, complete 25-card numerical result, actual owner, or full-width actual-context quotient value exists; v1, v2, and v3 are permanently consumed by bootstrap, serializer, and resource-inspector terminals respectively; no general odd-chip or side-pot leaf automaton, repeated-actor multiway existence result, off-tree opponent-action result, cross-street belief and certificate handoff, certified full-width river strategy bridge, sealed blueprint trainer/checkpoint/abstraction/slice-audit chain, trained blueprint, v0a/v0b integrated bot, preparation-bank filling result, frozen evaluation opponent pool, complete 15-second decision, production action width, or poker-strength result exists

## Question

What did the sole ADR-0402 invocation establish, where did it stop, and may its
resource inspection be repaired in place or replayed as the same campaign?

## Decision

Retain the exact V3 artifact, permanently close its owner, and reject the
resource-inspector seam. Do not edit or invoke the V3 adapter, runner, reader,
controls, config, or immutable scientific source; do not delete, overwrite,
rename, or regenerate the artifact. The independent reader and its nested V2
and V1 compatibility views all reconstruct the same scientific terminal:

```text
terminal=compiler_or_primitive_rejection
passed=False
event_count=5
handshake=True
serializer_probe=True
phase_count=0
projection=None
```

The seven-record journal is complete. Provenance binds clean source commit
`ee42b8d60efb7f7334a4c7beff1a295cf1d19795`, every V1/V2/V3 source and
config dependency, both prior artifacts, and absence of the reserved actual
path. The fresh-challenge literal bootstrap passed. The real no-CUDA probe
then forced `RuntimeError: forced_serializer_probe_compiler_failure` through
the unchanged scientific `run_calibration_preflight.send` closure. Its exact
six-field slots `CudaRuntimeIdentity`, reason, scientific terminal, and complete
restoration map survived the adapter, shared child transport, durable journal,
and independent reader. ADR-0400's serializer defect is therefore retired in
V3.

The actual campaign next loaded the named RTX 5080 runtime. The immutable call
order establishes only the following successful predecessors: NVRTC returned
an ELF payload, `Module.load` accepted it, all named kernels were obtained, the
driver resource report returned, CUDA 13.3 `cuobjdump --version` returned an
accepted identity, and the cubin bytes were written to the named temporary
file. The next exact command was:

```text
cuobjdump.exe --dump-resource-usage
    C:\Users\point\AppData\Local\Temp\tmp3fju199y.cubin
```

It returned nonzero exit status `4294967295`. Because the frozen source used
`check=True`, the exception path retained neither stdout nor stderr. Its
`finally` removed the temporary cubin, and the fresh child then exited. The
artifact therefore contains no exact cubin hash, raw resource output, parsed
register row, stack/local row, or driver row. The command and return status are
known; why the tool rejected this cubin is not. Calling it a malformed cubin,
an SM120 incompatibility, a tool crash, or a parser defect would be causal
invention.

This is a compiler/resource-*instrument* rejection, not evidence that NVRTC
failed and not a capacity result. The ELF and module-load predecessors show
that compilation progressed farther than ADR-0400 could establish, but no
resource gate was evaluated. No calibration fixture was constructed after the
inspection block, no population 10 or 22 value exists, and no phase or
projection exists. The approximately seven-second outer command wall is
lifecycle time, not a primitive, iteration, solve, per-street, or action-clock
measurement.

The correct successor is diagnostic-first and additive. Before another
capacity campaign, a new identity must retain the exact bounded cubin and raw
outcomes from candidate inspectors without `check=True` erasing their streams.
The qualification must distinguish command availability from semantic
adequacy: a candidate passes only if its raw output names all three direct
kernels, supplies the prospectively frozen register and stack/local quantities,
is independently reparsed, and can be conservatively combined with driver
attributes under ADR-0395's maxima rule. CUDA 13.3 `cuobjdump` resource usage,
its ELF dump, and CUDA 13.3 `nvdisasm` are candidates to test, not assumed
authorities. If none qualifies, close the route; do not fall back silently to
driver-only evidence or rename a nearby quantity.

This is the third consumed work-preflight invocation before calibration: V1
failed worker bootstrap, V2 exposed the typed serializer defect while masking
its antecedent, and V3 passed both repairs before exposing the resource tool.
Together the immutable journals contain `5322 + 8508 + 15783 = 29613` bytes.
The shared runner harness retired the first two defect instances and made the
third antecedent durable, but it has not yet reduced the lane's first-real-call
tax to zero. This cadence still honors every kill criterion because each owner
was called once, every first terminal was retained, no threshold moved, and no
failed identity was revived.

The inherited front-door trust chain remains explicit. ADR-0310 made native-
simplex robustness the next systems question. ADR-0311's directive is
Preregister the native-simplex robustness audit. ADR-0312's directive is Seal
the native-simplex audit compiler and corpora. ADR-0313's directive is Seal
the native-simplex audit runner before results. ADR-0314's decision is Retain
the native-simplex audit and reject the frozen gate. ADR-0315's directive is
Source-seal the artifact-only native-simplex gate correction. ADR-0316's
decision is Accept the corrected audit and bound replacement eligibility.
ADR-0317 separates solver classes; ADR-0318 binds HiGHS 1.12.0; ADR-0319
requires one public HiGHS-DS call per canonical task; and All 177 ordered
observations pass under ADR-0320. ADR-0321 preserves caller-owned legal
fallback, ADR-0322 returns research evidence or rejection with no action,
ADR-0324 remains value-unopened, and ADR-0325 was authorized exactly once.
ADR-0326/0327 govern the exhaustive bounded development-teacher; ADR-0328
retains it and the solver-free rebinder; ADR-0330 remains permanently closed;
ADR-0331's append-and-fsync discipline, ADR-0332's exclusive `xb` open, and
ADR-0333's No replacement sizing value was opened statement remain binding.
ADR-0334/0335 bind the 2,113-task non-replay chain; ADR-0336 records width
three; ADR-0337 owns the response-closed direct mechanism; ADR-0338 alone
records the selected development raise width; and ADR-0339 remains a finite
absence claim. ADR-0340's 192 prospective tasks remain distinct from
ADR-0341's 94 accepted one-call arms and ADR-0343's 126 confirmation arms;
ADR-0342 alone authorized the retained confirmation. ADR-0344/0345 own the
finite h4 responder-raise keystone. ADR-0346/0347 lead only to responder-row
growth; ADR-0348/0349 lead only to selector-window work. ADR-0350 opened
selector-stable affine integration; ADR-0351 replaced it with tie-aware legal
h4 affine envelopes; ADR-0352 is closed by ADR-0353; ADR-0354/0355/0356 own
the factorized exact face result; ADR-0357/0358/0359 own and close same-fixture
integration; and ADR-0360/0361/0362 alone own the untouched confirmation and
assessment. ADR-0363 and ADR-0365 remain consumed; ADR-0364 remains exactly
`GetProcessMemoryInfo failed`; ADR-0366 remains exactly
`representation_rejected_before_target_allocation` with zero target calls.
ADR-0367 through ADR-0384 own and close the quotient algebra, capacity,
bounded CUDA, staged, liveness, validation, and literal-target ladder.
ADR-0368 seals only the exact bounded algebra keystone for the occupied-card
quotient within that chain. ADR-0369 is the source-only arithmetic boundary.
ADR-0370 seals the numeric-array and logical-work result. ADR-0378 remains the
accepted prospective source-only literal-target liveness and streamed-
validation boundary, and ADR-0379 retains its 244,970,204-byte device-cap
margin without live admission. ADR-0385 froze the legal bridge question; ADR-0386
source-sealed only its host answer; ADR-0387 froze the consumer-capacity
question; ADR-0388 source-sealed only that CuPy-free answer; ADR-0389 froze
the additive CUDA-consumer question; ADR-0390 rejected its source seal;
ADR-0391 froze the first paired-tile boundary; ADR-0392 corrected its pre-
source arithmetic completeness; ADR-0393 retained the first implementation as
a wall rejection; ADR-0394 freezes the work-preflight question; ADR-0395
corrects only its resource instrument before result; ADR-0396/0397 own and
close V1; ADR-0398/0399/0400 own and close V2; ADR-0401/0402/0403 own and
close V3. No earlier owner is revived.

For machine-checked continuity, ADR-0317's directive remains Separate solver
classes and prioritize the certified sizing adapter. All 177 ordered
observations pass under ADR-0320, making the separate consumer eligible.
ADR-0326 and ADR-0327 govern the exhaustive bounded development-teacher
chain. ADR-0328 retains that exhaustive teacher and solver-free rebinder.
ADR-0334 and ADR-0335 bind the 2,113-task non-replay chain. ADR-0344 and
ADR-0345 own the finite h4 legal responder-raise keystone line. ADR-0346 and
ADR-0347 lead only to responder-row growth. ADR-0348 and ADR-0349 lead only
to selector-window work. ADR-0351 requires the tie-aware legal h4 affine-
envelope. ADR-0354 through ADR-0359 own the factorized face and affine
consumer chain. ADR-0380 freezes the complete ordered populations 10 and 22.
ADR-0383's owner was invoke exactly once and remains consumed by ADR-0384.
ADR-0403 imports neither that owner nor its target. The phrases exclusive
untouched legal h4, selector-window, 2,113-task, exhaustive bounded
development-teacher, response-closed direct mechanism, and caller-owned legal
fallback retain their prior meanings.

The exact historical continuity strings remain explicit. ADR-0328 retains the
exhaustive teacher and solver-free rebinder before the direct closed
finite-block greedy line. ADR-0351 requires the tie-aware legal h4 affine-envelope.
ADR-0352 remains closed before any fresh untouched tie-aware affine result.
ADR-0353 precedes ADR-0354's factorized exact active-set directional-face
diagnostic. ADR-0355's owner was invoke exactly once; ADR-0356 retains that
directional-face diagnostic before tie-aware affine integration. ADR-0357
requires an exclusive legal h4 owner, ADR-0358 was invoke exactly once, and
ADR-0359 requires a fresh value-unopened confirmation. ADR-0382 preregistered
the literal-45 config; ADR-0383 source-sealed it; ADR-0384 closed it.

### Kill criteria for a successor

Kill any successor that edits or invokes V1, V2, or V3; deletes, overwrites,
renames, or treats an artifact as retryable; reuses a consumed lifecycle
identity; fails to retain the exact cubin or raw nonzero inspector streams;
uses `check=True` before evidence capture; assumes a tool is semantically
adequate because `--version` passes; uses driver-only rows after the dual-
instrument contract; changes the five caller compiler options, CUDA source,
population, arithmetic, phase, ratio, resource ceiling, wall, or claim;
constructs population 25 numerically; or promotes inspection, compilation, or
systems evidence into capacity, action-clock, quality, truncation, blueprint,
or poker-strength evidence.

### Claims boundary

ADR-0403 establishes that V3 passed literal bootstrap and exact-type serializer
coverage, then the actual CUDA 13.3 resource-usage command rejected the
temporary cubin with the retained status. It establishes no causal explanation
for that status, raw resource value, resource-gate verdict, calibration,
projection, complete 25-card value, actual 45-card value, resolver iteration,
solve, action, 15-second result, decision quality, truncation, blueprint, or
poker strength. Systems evidence remains no quality prior.
