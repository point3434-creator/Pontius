# ADR-0470: Retain the accidental v5 preauthorization attempt and preregister v6

- Status: accepted accidental-attempt retention and fresh-lifecycle preregistration; a source-seal negative launcher control inherited `PYTHONSAFEPATH=1`, called the real v5 owner despite omitting `-P`, wrote the exact 606-byte v5 attempt marker, and then rejected at absent authorization before any child launch, scientific import, compiler call, device access, timing value, or result; v5 is permanently closed, its marker is retained byte-exactly, and only a fresh v6 implementation may proceed
- Date: 2026-08-27
- Follows: ADR-0469
- Recovery config: `experiments/configs/legal-river-quotient-compiled-global-separation-calibration-v8-v5-attempt-recovery.json`
- Recovery config canonical-LF SHA-256: `5fab5270880625e8f909fd7f1a334f6fa38471ca34bd453de47d93fbfd2413ff`
- Retained v5 attempt: `artifacts/work_preflight/legal_river_quotient_compiled_global_separation_calibration_v5.attempt.json`
- Retained v5 attempt bytes / raw SHA-256: `606 / 104820d0c67391365d18fb76ca72c704e40e467e2993a96c618d4bf91155600d`
- V5 public owner entries / v5 result rows / v5 launch markers: `1 / 0 / 0`
- V4 public invocations / v4 result rows / v4 attempt markers / v4 launch markers: `0 / 0 / 0 / 0`
- Compiler calls / CuPy scientific imports / device queries / allocations / module loads / kernel launches / timing values: `0/0/0/0/0/0/0`
- Candidate / topology / arithmetic schedule selected: `null / null / null`
- Front-Door-Kind: controller-v1
- Front-Door-Research: ADR-0280
- Front-Door-Process: ADR-0470
- Front-Door-Contract: ADR-0307
- Front-Door-Revoked: ADR-0281, ADR-0468
- Front-Door-Active-Next: Implement and source-seal only the fresh v6 lifecycle frozen here; preserve the exact v5 attempt marker and every v4/v5 absence predicate; use fresh protocol, campaign, result, attempt, launch-marker, and authorization identities; pass every source-only control with zero skips; stop before any public v6 owner, compiler, CuPy scientific import, device access, timing, or result
- Front-Door-Blockers: no correct v6 source seal, invocation authorization, or v6 result exists; v5 is permanently closed after one accidental preauthorization attempt and v4 is permanently closed uninvoked; no production source-local base producer, refresh cadence, exponent admission, or width bound exists; no selected topology, population-25 result, actual-45 numerical result, global resolver-certificate integration, complete resolver iteration, known certificate count per action, or 15-second action result exists; no repeated-actor multiway existence result, off-tree opponent-action result, cross-street belief and certificate handoff, certified full-width river strategy bridge, trained blueprint, integrated bot, production action width, or poker-strength result exists

## Question

Did the v5 source-seal audit leave a fresh owner that can be authorized and
fired, and if not, what is the narrow recovery?

## Decision

No. Retain the exact v5 attempt and permanently close v5. During the combined
v4/v5 source-only suite, the negative launcher control created a child without
an explicit `-P` argument and then called the real launcher's private owner.
The child inherited `PYTHONSAFEPATH=1` from the correctly isolated parent, so
`sys.flags.safe_path` was true. The module-level launcher guard therefore
accepted the child, the v5 public owner entered, and the exclusive attempt
marker was durably written. The inherited owner then assessed the retained v2
outcome and checked rejected-v3 result absence. Both completed. Authorization
lookup then raised `FileNotFoundError: v5 invocation authorization is absent`.

The exact retained marker is a regular non-symlink, non-reparse 606-byte file
with raw SHA-256
`104820d0c67391365d18fb76ca72c704e40e467e2993a96c618d4bf91155600d`.
Its canonical payload binds the v5 protocol, campaign, result path, closed v4
result path, and rejected v3 result path frozen by ADR-0469. No v5 result,
pending launch, consumed launch, aborted launch, or v5 authorization-config
path exists. Every v4 result,
attempt, and launch path remains absent, as does the rejected-v3 result. The
stop occurred before child-launch
transition, host activation, configured parent execution, scientific or CuPy
import, compilation, device query, allocation, module load, kernel launch,
timing value, scientific row, or topology work. Those absence statements are
scoped to the exact control path and artifact inspection; they are not a
systems or numerical result.

The accidental attempt ran from unsealed mutable source that was corrected
afterward. The retained marker proves consumption and its exact payload; it
does not prove the exact identity of every source byte that executed.

The defect is a semantic-proxy error. The property under test was effective
safe-path state, but the control treated absence of the `-P` argv token as
proof that safe-path was false. Environment configuration can establish the
same interpreter state. Result absence cannot restore the consumed attempt.
A test-induced entry is still an entry, so neither a renamed authorization nor
a repaired test may revive v5.

Retain the repaired v5 source and tests as diagnostic evidence, not as an
invocable owner. They now place the effective `-B`/safe-path guard immediately
after builtin `sys` and before `pathlib` or any repository import; acquire
the inherited v4 lock before the wrapper lock; use `os.path.lexists` for
permanent absence; bind authorization bytes to a regular non-reparse working
file and its committed Git blob; restore lifecycle predicates in `finally`;
validate imported-but-unstarted science truthfully; and require exact Python
types at inherited success comparisons as well as rejection comparisons. The
corrected v5 focused suite passes 18/18 against the retained attempt, but that
does not unconsume it.

Open only a fresh v6 lifecycle after this decision and recovery config are
committed. V6 receives fresh protocol, campaign, public result, attempt,
child-launch, and authorization identities. It may reuse unchanged v4 mechanics.
Corrected v5 validation logic must be copied or independently reimplemented
without acquiring a v5 binding lock; v6 must not call the v5 public assessor.
Before source seal, every source-only
test must avoid every production public `main`. Unsafe-launcher controls must
scrub both `PYTHONPATH` and `PYTHONSAFEPATH`, exercise only a copied launcher
with a poisoned non-builtin import, require the exact pre-import
`RuntimeError: v6 retained-attempt launcher requires Python -B -P`, and prove
the retained marker bytes did not change. The real launcher may be
source-imported under isolation but neither
its private nor public owner may be called.

Every v6 public and child boundary must require the exact retained v5 marker,
absence by `lexists` of the v5 result, authorization and all launch paths,
absence by `lexists` of the rejected-v3 result and every v4 lifecycle path, and
absence by `lexists` of every real v6 lifecycle and authorization path through
source seal, before and after normal or exceptional delegation. Bindings acquire
v4 before v6 and never acquire a v5 lock. The source seal must prove the exact
replacement-key set and every installed value; recursively mutate every mapping
domain in the actual layered header; reject all Boolean/integer aliases in
rejection and success records; key executed-module identity to science actually
started rather than merely imported; run real authorization parsing and Git
validation against injected exact responses; and exercise lifecycle transitions
only against temporary paths.

The live authorization remains separate. Its config must use the frozen v9
path and `pontius-adr0472-one-commit-v6-authorization-v1` schema and be a regular
non-reparse file whose canonical-LF working bytes equal the committed Git blob,
and only the exact one-generation ancestry gate may remain dormant at source
seal. A later authorization commit may not change any sealed implementation,
test, retained attempt, or lifecycle byte. No public v6 owner is authorized by
this ADR.

The inherited front-door trust chain remains explicit. ADR-0310 made native-
simplex robustness the next systems question. ADR-0311's directive is
Preregister the native-simplex robustness audit. ADR-0312's directive is Seal
the native-simplex audit compiler and corpora. ADR-0313's directive is Seal the
native-simplex audit runner before results. ADR-0314's decision is Retain the
native-simplex audit and reject the frozen gate. ADR-0315's directive is
Source-seal the artifact-only native-simplex gate correction. ADR-0316's
decision is Accept the corrected audit and bound replacement eligibility.
ADR-0317 separates solver classes; ADR-0318 binds HiGHS 1.12.0; ADR-0319
requires one public HiGHS-DS call per canonical task; and All 177 ordered
observations pass under ADR-0320. ADR-0321 preserves caller-owned legal
fallback, ADR-0322 returns research evidence or rejection with no action,
ADR-0324 remains value-unopened, and ADR-0325 was authorized exactly once.
ADR-0326/0327 govern the exhaustive bounded development-teacher; ADR-0328
retains it and the solver-free rebinder before the direct closed finite-block
greedy line; ADR-0330 remains permanently closed; ADR-0331's append-and-fsync
discipline, ADR-0332's exclusive `xb` open, and ADR-0333's No replacement
sizing value was opened statement remain binding. ADR-0334/0335 bind the
2,113-task non-replay chain; ADR-0336 records width three; ADR-0337 owns the
response-closed direct mechanism; ADR-0338 alone records the selected
development raise width; and ADR-0339 remains a finite absence claim.
ADR-0340's 192 prospective tasks remain distinct from ADR-0341's 94 accepted
one-call arms and ADR-0343's 126 confirmation arms; ADR-0342 alone authorized
the retained confirmation. ADR-0344/0345 own the finite h4 responder-raise
keystone line. ADR-0346/0347 lead only to responder-row growth; ADR-0348/0349
lead only to selector-window work. ADR-0350 opened selector-stable affine
integration; ADR-0351 replaced it with tie-aware legal h4 affine envelopes;
ADR-0352 is closed by ADR-0353; ADR-0354/0355/0356 own the factorized exact face
result; ADR-0357/0358/0359 own and close same-fixture integration; and ADR-0360/
0361/0362 alone own the untouched confirmation and assessment. ADR-0363 and
ADR-0365 remain consumed; ADR-0364 remains exactly `GetProcessMemoryInfo failed`;
ADR-0366 remains exactly `representation_rejected_before_target_allocation`
with zero target calls. ADR-0367 through ADR-0384 own and close the quotient
algebra, capacity, bounded CUDA, staged, liveness, validation, and literal-
target ladder. ADR-0380 freezes the complete ordered populations 10 and 22.
ADR-0381 source-seals the one-shot literal-45 CUDA owner. ADR-0385
preregisters the actual-context quotient bridge; ADR-0386 records the source-
sealed actual-context quotient bridge; ADR-0387 freezes the consumer-capacity
seam; and ADR-0388 records the source-sealed CuPy-free consumer-capacity result.
ADR-0389 remains the accepted prospective actual-context quotient CUDA-consumer
boundary; ADR-0390 rejects its source seal; ADR-0391 remains an accepted
prospective bounded-arithmetic boundary and freezes the first paired-tile
boundary; ADR-0392 remains the accepted prospective preregistration-completeness
correction and corrects its pre-source arithmetic completeness; ADR-0393
remains the accepted bounded-device source-seal rejection and retains the first
implementation as a wall rejection. ADR-0390 and ADR-0393 retain the exact
historical status label accepted bounded-device source-seal rejection.
ADR-0394 through ADR-0416 own and close the paired work-preflight, exact-cubin
qualification, and retained capacity rejection. ADR-0417 through ADR-0432 own
and close the shared-direct successor and artifact-only capacity rejection.
ADR-0433 through ADR-0435 own the captured-pair exact-integer keystone.
ADR-0436 freezes the fixed-width comparison, ADR-0437 corrects its provenance
control before source, ADR-0438 source-seals both CPU candidates, ADR-0439
opens only the separate compiled-device preflight, ADR-0440 corrects its phase
topology before source seal, ADR-0441 source-seals that corrected owner,
ADR-0442 consumes its compiler rejection, ADR-0443 and ADR-0444 own the first
MSVC recovery, ADR-0445 consumes its zero-event terminal, ADR-0446 freezes the
split-environment successor, ADR-0447 source-seals it without compilation or
device work, ADR-0448 alone retains its passing first device-preflight
terminal without selecting a candidate or opening actual-45 numerics,
ADR-0449 freezes the artifact projection, ADR-0450 corrects its runtime and
artifact accounting, ADR-0451 seals the projector, ADR-0452 retains its
rejection, ADR-0453 freezes the selective-separation mechanism, ADR-0454
source-seals it without opening a result, ADR-0455 freezes the base-first four-
topology successor while closing that result owner uninvoked, ADR-0456 source-
seals the reduced successor without opening its bake-off, ADR-0457 freezes the
compiled calibration, ADR-0458 source-seals it, ADR-0459 consumes its sole
pre-owner failure, ADR-0460 freezes the absolute-Git recovery, ADR-0461 source-
seals that recovery, ADR-0462 retains its launch-arity rejection, ADR-0463
opens the fresh arity source lane, ADR-0464 corrects that lane before source
seal, ADR-0465 records the rejected v3 source seal, ADR-0466 closes v3
uninvoked, ADR-0467 source-seals the deferred-import v4 lifecycle, and ADR-0468
authorizes only its exact one-generation invocation identity without firing it.

For machine-checked continuity, ADR-0317's directive remains Separate solver
classes and prioritize the certified sizing adapter. All 177 ordered
observations pass under ADR-0320, making the separate consumer eligible.
ADR-0326 and ADR-0327 govern the exhaustive bounded development-teacher chain.
ADR-0328 retains that exhaustive teacher and solver-free rebinder. ADR-0334 and
ADR-0335 bind the 2,113-task non-replay chain. ADR-0344 and ADR-0345 own the
finite h4 legal responder-raise keystone line. ADR-0346 and ADR-0347 lead only
to responder-row growth. ADR-0348 and ADR-0349 lead only to selector-window
work. ADR-0351 requires the tie-aware legal h4 affine-envelope. ADR-0354 through
ADR-0359 own the factorized face and affine consumer chain. ADR-0380 freezes
the complete ordered populations 10 and 22. ADR-0383's owner was invoke exactly
once and remains consumed by ADR-0384. The phrases exclusive untouched legal
h4, selector-window, 2,113-task, exhaustive bounded development-teacher,
response-closed direct mechanism, and caller-owned legal fallback retain their
prior meanings.

The exact historical continuity strings remain explicit. ADR-0352 remains
closed before any fresh untouched tie-aware affine result. ADR-0353 precedes
ADR-0354's factorized exact active-set directional-face diagnostic. ADR-0355's
owner was invoke exactly once; ADR-0356 retains that directional-face
diagnostic before tie-aware affine integration. ADR-0357 requires an exclusive
legal h4 owner, ADR-0358 was invoke exactly once, and ADR-0359 requires a fresh
value-unopened confirmation. ADR-0367 preregisters the occupied-card quotient.
ADR-0368 seals the exact bounded algebra keystone. ADR-0369 freezes the
source-only arithmetic boundary. ADR-0370 seals the numeric-array and logical-work
result. ADR-0378 freezes the source-only literal-target liveness boundary.
ADR-0379 retains the 244,970,204-byte margin. ADR-0382 preregistered the
literal-45 config. ADR-0386 records the source-sealed actual-context quotient
bridge. ADR-0405 remains an accepted source seal.

### Recovery boundary

ADR-0470 retains one accidental v5 preauthorization attempt and freezes only
the fresh v6 source/lifecycle requirements. It authorizes neither a v6 source
seal nor an invocation. V4 remains closed uninvoked; v5 remains closed after its
attempt even though neither has a scientific result.

### Kill criteria

Kill v6 before source seal if any test calls a production public owner; any
launcher guard follows a non-builtin import; an unsafe control inherits either
Python path environment variable; the retained v5 attempt differs by one byte;
the retained marker is a symlink, reparse point or non-regular file; any
v3/v4/v5 or premature-v6 absent path exists even as a dangling link; any public or child
exception can escape without rechecking retained state; binding order can
invert; an actual layered-header mapping node or bool/int alias lacks a
re-chained mutation; live authorization parsing or Git-blob binding is replaced
by a no-op; v5 public assessment is reused; or any compiler, scientific import,
device, timing, or v6 result work opens before a separate clean authorization.

### Claims boundary

ADR-0470 records only the exact accidental attempt, its preauthorization stop,
the corrected source-only diagnostics, and a prospective v6 lifecycle. It
supplies no compiled calibration, numerical, resource, speed, topology, target,
resolver, action-clock, decision-quality, truncation, blueprint, or strength
result. The no-science statement is an exact-path absence claim, not global
certification. The retained marker does not identify the exact unsealed source
bytes executed by the accidental attempt.

## Consequences

- The 606-byte v5 attempt remains immutable retained evidence.
- V5 can never be authorized, retried, or relabeled as fresh.
- V4 remains permanently uninvoked and every v4 lifecycle path remains absent.
- V6 source work may begin only from this committed preregistration.
- Any future v6 authorization must be a separate sole-child commit of its source seal.
