# ADR-0505: Open the selectable decision-provider source round

- Status: accepted source-opening decision upon its separately authorized commit
- Date: 2026-09-06
- Follows: ADR-0504
- Base-Commit: e205cd8cd6f46a50db8b2d0cb1f39366da0f2767
- Invocation-Authority: finite named correctness controls after adoption; no demo or operating run
- Front-Door-Kind: controller-v1
- Front-Door-Research: ADR-0280
- Front-Door-Process: ADR-0505
- Front-Door-Contract: ADR-0307
- Front-Door-Revoked: ADR-0281, ADR-0468, ADR-0472, ADR-0475
- Front-Door-Active-Next: Implement the selectable provider and baseline through a complete session
- Front-Door-Blockers: provider source acceptance pending; operating and research prerequisites open

## Decision

Open the CPU-only selectable decision-provider increment under the approved design
in docs/architecture/v0a-decision-provider-r001/brief.md and design.md, the exact
source-contract.md in that directory, and the companion implementation plan at
docs/superpowers/plans/2026-09-06-decision-provider.md. The controller approved
the reviewed design and scoped prospective source supersession. This decision
requires its own exact commit authorization; a draft or design approval alone
does not adopt it. No source implementation runs before that adoption.

The outcome is one selectable baseline operating through the real runtime,
event interface, host and multi-hand session. A provider-only library is incomplete.
The engine keeps legal state, visible-state admission, the continuous action wall,
fallback, application and delivery. The baseline follows the fixed approved rules;
it introduces neither a trained policy nor a claim of strategic improvement.

The source contract is the precise refinement of the approved architecture.
It gives abstention an explicit null-action representation, distinguishes skipped,
late, invalid and exceptional provider responses, specifies six-card evaluation,
and preserves the historical rehearsal driver without reassigning its source seal.
An additive codec file keeps new records separate from the old model/trace format.
The approved design copies remain unchanged records of the architecture review.

## Bounded prospective source exceptions

Prospectively supersede CLAUDE.md rule 1 only for the twelve current file versions
listed with exact base blobs and permitted changes in source-contract.md. The five
production exceptions are the hand runtime and current hand/event/host/session
tools. Six registration exceptions and one existing test's host-binding refresh
complete the list. Recheck raw base blobs before the first source edit. All other
old source, tests, fixtures, decisions and retained artifacts remain byte-identical.
This is no permanent permission to edit those paths or their future versions.

Add only the five provider-package files, four new suites and two new fixture
files named in the contract. Preserve the existing model, trace parser, action
clock, betting spine, kernel, showdown evaluator, seeded generator and historical
rehearsal driver. Legacy blueprint behavior and wire records remain unchanged;
baseline mode uses the new exact provider protocol. Existing consumers never
interpret provider actions as blueprint hits or certified resolver decisions.

The four current tool admissions use the contract's finite source population and
explicit supersession sets. Source manifests identify actual committed bytes;
they do not grant execution or claim adoption. No directory-wide exception,
unrelated pin relaxation, import bypass, analyzer repair or new capability grant.
Only the explicit new import edges are allowed. Old dependency edges/SCCs, test
IDs, assertions, payload order and CI gates are preserved.

## Correctness, acceptance and limits

After adoption, allow only the finite correctness controls and unchanged regression
suites listed in the contract. They may exercise the real provider/runtime and
child-process session in fresh disposable D-local snapshots. Fault schedules act
at named seams and must expose incorrect behavior through the real boundary.
No uncontrolled population sampling, seed reroll, outcome-selected fixture,
automatic watch session or poker execution beyond those tests follows.

Keep 1,200 added/removed production lines, 1,800 added test lines, 160 manual
registration added/removed lines, and 32 KiB total new fixtures as the source
ceilings. Count comments, whitespace and all provider integration code; generated
inventory/profile output and this documentation-only decision are excluded from
those line counts. One initial source candidate and one bounded correction;
return before a third or before exceeding scope. Test and transport coverage
justify the larger test allowance; no generic proof/plugin/process framework.

Source acceptance requires both independent Tier C CLEAN reviews followed by the
named checks on actual CPython 3.11.15 first, then 3.14.6. Use fresh exact-candidate
D-local snapshots, -B -P, scrubbed environments, exact cwd/src, origin checks and
absolute Git. Preserve every refusal and complete census assertion chain. Source
seal/adoption is a later separately authorized decision commit, not this opening.

This proposal's delta is exactly this ADR, the two unchanged approved design
documents, source-contract.md, the implementation plan and generated STATUS.md.
Freeze all six together, obtain two fresh independent Tier C CLEAN reviews, then
run unchanged status --check and all twelve status tests on actual 3.11.15 first,
then 3.14.6 in fresh exact-candidate snapshots. Verify raw exception base pins and
approved design hashes. No provider, poker or inventory payload runs in this
documentation task. One initial proposal and at most one bounded correction.

The original ADR-0504 attempt stopped in preflight; the controller then explicitly
authorized a fresh demonstration retry. Its three-hand completion is retained at
D:/Pontius/tmp/v0a-seeded-watch-run-r002/. Neither demonstration is strategy or
acceptance evidence. Preserve both roots and all original output without changes.
No new demo, operating budget, training, neural loading, self-play, league, H32,
compiled work, analyzer repair, research owner, cleanup or ref retirement opens.
