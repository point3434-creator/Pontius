# r001 disposition

Accepted P2: successful summaries omitted the promised per-case differences and
mixed-sign reporting. The original FAIL verdict and sealed inventory are preserved
in reviews/. The two replaced source members are retained in reviewed-source/ and
match the original identity. All other identity members remain unchanged.

Correction: r002 adds a deterministic report over the reconciled records, with
method-minus-reference differences against both controls, sign counts and mixed
classification at every checkpoint. It declares the final checkpoint separately.
Chip and pot-fraction differences are retained. No solver, feature, grouping,
population or execution behavior changes. A synthetic mixed/zero-sign regression
failed before the correction; it and the real development campaign now pass.

The CRLF observation concerns preserved registry bytes; no normalization is needed.
One existing reviewer rechecks this correction. No holdout has been evaluated.
