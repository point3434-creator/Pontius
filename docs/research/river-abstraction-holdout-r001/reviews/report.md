# Independent review 01 — river holdout runner r001

Specification verdict: **FAIL**. Engineering verdict: **FAIL for the bounded reporting omission below**. The admission, binding, preservation and tested failure paths passed the focused checks; this is not a finding that the poker mathematics or CFR changed.

## Material finding

### P2 / medium, high confidence — complete campaign reports omit the declared comparison results

Location: `D:/Pontius-worktrees/eval-runner-consolidation/tools/river_abstraction_campaign.py:208-214`.
Requirement: `docs/research/river-abstraction-holdout.md:16-19` requires primary candidate-versus-uniform-equity and secondary candidate-versus-range-equity comparisons, retention of every per-case difference, and reporting mixed signs as mixed results.

The successful completion path constructs only raw per-method means, unchanged case records and receipts. Neither that builder nor the single-case record producer computes candidate-minus-baseline differences or a mixed-sign classification. No other reporting consumer is included in this identity. Consequently every normally completed campaign writes `complete: true` without the required explicit differences; if the candidate's per-case differences have both signs, nothing reports that comparison as mixed. Raw inputs permit a later analyst to compute this information, but they do not satisfy the declared retention/reporting requirement.

Reachable scenario: any valid successful development or holdout campaign reaches this builder. This omission is unconditional for difference fields; the mixed-sign omission matters when cases disagree. No holdout outcome is assumed or was evaluated.

Evidence: direct read of the entire new runner and driver, with the exact affected lines recorded in the review tool transcript. The fresh development campaign test exercises the same success builder but asserts only completion, 16 records and a `10` checkpoint mean; it does not assert the comparison contract (`tests/test_river_abstraction_holdout.py:111-114`).

Smallest remediation direction: add a deterministic report transformation over reconciled records that retains per-case candidate-minus-uniform and candidate-minus-range-equity differences, declares the final-checkpoint equal-weight comparisons, and explicitly labels mixed per-case signs. Preserve earlier checkpoints and fixed interpretation. No source changes were made in this pass.

Falsifier: identify an output member generated by this frozen identity that contains both required per-case difference series and mixed-sign classification, or demonstrate such fields in the returned summary from this exact completion path. The inspected producer has no such fields or computation. Existing raw method records alone do not falsify the finding.

## Fresh evidence and requirement mapping

| Requirement | Evidence and result |
| --- | --- |
| Exact review identity | Eight SHA-256 members matched at opening and finish; branch `codex/river-abstraction-holdout`, HEAD/base `1b4d1a0e26cd4da90ff74678de2e48ef53ec5599` matched. Final evidence in `identity-final.json`. |
| Admission and fixed boards | Declared integer tuples match the specification's card encoding. Default development refusal and explicit admission tested; mocked holdout preparation/driver dispatch stops before evaluation. Pass within this boundary. |
| Existing features/grouping/CFR/payoff preservation | Base diff confines the library change to the new holdout constants, wrapper and admission gate; subsequent hand/range selection remains unchanged. Driver solver/group loops are unchanged apart from normalized reporting and dispatch/metadata. Fresh generic evaluator, generic full-tree/grouped CFR, normal-form oracle, tie, feature and capacity tests passed. |
| Bound plan without execution | Exact packet bytes parsed with duplicate-key/nonfinite-aware parser and validated; plan digest matched identity; all eight computational plan pins matched live sources; Python 3.14.6 and NumPy 2.5.2 matched. No evaluation/execution entry point called in this check. |
| Fixed grid/population | Plan validates exact ordered four cases, strict integer population/schedule and fixed holdout 96/10000. Reconciler independently rebuilds declared inputs, verifies joint/ranges/hands/groups/population and checks exact method/checkpoint set and count. Holdout 48-profile requirement follows from static checkpoint/grid logic; actual holdout runtime is deliberately untested. |
| Equal capacity and lifted full/restricted evaluation | Recomputed input groups must exactly match bound preparation; policy lengths/domains are checked by evaluator, lifted rows are checked, and all full/restricted metric dictionaries and pot fractions are recomputed. Existing capacity, lift/evaluation and metric-tamper tests passed. |
| One-shot digest/source/environment binding | Exact digest/source mismatch tests refuse before output creation; existing output is refused and successful summary bytes remain unchanged. Static trace shows environment check precedes reservation, original bytes copied, no run override, and source checks before each child and completion. |
| Sequential bounded worker/failure paths | Existing real development campaign runs four children sequentially. Mock nonzero/timeout tests launch once, stop the sequence and retain a failure marker without summary. Source enforces 60-second child timeout and one-thread BLAS environment, records stdout/stderr, retains returned-child exit receipts and preserves timeout output. |
| Complete reporting | Raw profiles, means, bounds/gains and pot fractions are retained; declared differences/mixed comparison reporting are missing. Fail, finding above. |
| Completion/publication | Static ordering requires all four reconciliations and final source equality before summary; final manifest follows summary. Specification correctly requires manifest plus successful exit, so a partial summary alone is not declared sufficient. |
| Interpretation | Specification and emitted driver limits avoid six-max strength, generalization, sampling confidence intervals and asymptotic-floor claims. |

## Executed commands and results

All Python commands used `D:/Pontius-worktrees/eval-runner-consolidation/.venv/Scripts/python.exe`, `-B`, and `PYTHONDONTWRITEBYTECODE=1`. The Python commands used the available escalation mechanism for Windows execution; no dependency installation occurred. All reviewer writes and redirected output were confined to `D:/Pontius/tmp/river-holdout-review-01`.

1. PowerShell `Get-Content` of handoff/identity, SHA-256 `Get-FileHash` over each identity member, Git `status --short`, `branch --show-current` and `rev-parse HEAD`: identity and tracked/new scope matched. Initial optional root `AGENTS.md` read reported absent; ancestor and scoped nested instruction discovery found none. Applicable user-supplied instruction was present in context.
2. Git `diff 1b4d1a0e26cd4da90ff74678de2e48ef53ec5599 -- .gitattributes src/pontius/river_abstraction_study.py tools/river_abstraction_study.py`, direct new runner/spec/plan reads: completed before inventory. Test registration diff and test source reads occurred only after inventory was written and hashed.
3. `python.exe -B --version`: exit 0, Python 3.14.6.
4. `python.exe -B -m pytest -p no:cacheprovider tests/test_river_abstraction_holdout.py tests/test_river_abstraction_study.py -k 'not test_development_strategy_and_groups_match_saved_prechange_smoke' --basetemp D:/Pontius/tmp/river-holdout-review-01/pytest-01 -q`: exit 0, **22 passed, 1 deselected in 5.41s**. TEMP and TMP both pointed to the reviewer directory. Full output: `pytest.txt`.
5. `python.exe -B -` with inline plan-only validation: loaded runner, called `parse_json`, `validate_plan`, `source_hashes`, compared exact plan digest to identity, asserted runtime pins; exit 0. Result: valid four-case/96-hand/10000-iteration plan, 48 statically declared profiles. Full output: `plan-validation.txt`. No `execute_plan`, `verify_case`, `study_case` or card evaluation was called.
6. Git `diff --check 1b4d1a0e26cd4da90ff74678de2e48ef53ec5599 -- .gitattributes src/pontius/river_abstraction_study.py tools/river_abstraction_study.py tests/cases.json`: exit 1, reports trailing whitespace on added `tests/cases.json:13`, whose displayed line ends in CR. Also warns about future LF-to-CRLF conversion of `.gitattributes`. This is a nonblocking whitespace/line-ending observation, not a functional failure or evidence of source mutation. No cleanup was performed.
7. Final eight-member `Get-FileHash`, Git branch/HEAD/status: all eight digests and identity values still matched; status unchanged. Saved `identity-final.json`.

Git commands used `C:/Program Files/Git/cmd/git.exe -c safe.directory=D:/Pontius-worktrees/eval-runner-consolidation -C D:/Pontius-worktrees/eval-runner-consolidation`; no Git configuration/index/HEAD mutation.

## Independence, ordering and limits

`inventory.md` was created and SHA-256 hashed before any test source or author check was opened. Inventory SHA-256: `a1e83f417c783a2625cf0bb8339f70a6e58149e3eeda393f277e5c1c62e7ae53`.

This was exactly one fresh reviewer pass with no subagents. Historical memory summary and user preferences were automatically injected, including earlier poker training/review descriptions and throughput claims. No memory files, ledgers, prior review/check directories, author scratch, or historical development outcome files were opened. The original allowed study specification itself contains historical baseline hash references; those references were seen as part of the contract, without following them. The parent additionally disclosed that author checks existed outside the eight identity members; their contents were not opened. This is not an unqualified coldness claim.

The saved-prechange-smoke parity test was deliberately deselected because it reads a prohibited historical result directory. No previous outcome was consumed indirectly by that test. Preservation assessment instead uses the actual base diff and fresh generic-kernel checks.

No holdout board equities/features/payoffs/policies were evaluated, and the bound packet plan was never executed. Full 96-hand/10000-iteration holdout timing, memory and numerical outcomes remain unmeasured. The actual development campaign check uses two hands and ten iterations, so multi-checkpoint completeness and full holdout population are supported by source/plan validation rather than a fresh full-size execution. Timeout tests inject `TimeoutExpired`; they do not measure a real 60-second process kill. The reconciler deliberately uses the same tested kernel, not an independently implemented solver. Final-manifest I/O failure, process hard kills, power loss, unrelated platform architecture and hypothetical hostile filesystem changes were not expanded into this review.

No production/test/config source was edited, no new tests or dependencies were added, and no retained experiment, adoption, commit, push, publication or holdout execution was performed. The candidate remained frozen across the recorded source identity checks.
