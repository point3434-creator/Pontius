# Independent invariant inventory (frozen before tests/check execution)

Target: identity.json in this directory; baseline ce73bf3e9cc996990e5fa5dbece8e16e14c8e0b7, branch codex/river-abstraction-study, checkout D:/Pontius-worktrees/eval-runner-consolidation. All five hashes, HEAD and branch matched before source assessment. Scope is the first development-candidate gate only.

Independence disclosure: automatically injected memory_summary text includes historical Pontius training/evaluator facts and user preferences; I have not used those facts as evidence or opened any memory file/index. No earlier review findings were supplied. Git status exposed the filename of the prohibited implementation plan, but its contents were not opened. No prohibited source contents were opened. Source evidence so far is the declared design, candidate kernel/driver, unchanged river.py, cfr.py, evaluation.py, game.py, pyproject.toml, and the baseline abstraction.py and evaluator.py. No test contents or checks have yet been opened/executed. Code-verification skill and its matrix reference were read; using-superpowers explicitly exempts dispatched subagents.

## Derived matrix and strategic invariants

1. Legal deal mass: each matrix entry is product of positive declared range weights if all nine cards are distinct, otherwise zero; total mass is one within floating-point error. Marginal support must include all and only hands with positive compatible mass. No method renormalizes or changes the deal population.
2. Terminal economics: after subtracting each player's equal share of the starting pot, check payoff is showdown sign times pot/2, bet-fold pays player 0 pot/2, and bet-call is showdown sign times (pot/2+bet). Ties yield zero showdown utility. Player 1 gets the exact negative. Verify via elementary terminal outcomes and generic tree returns.
3. Information-constrained best response: player 0 chooses one check/bet action for each own hand using conditional opponent behavior; player 1 chooses one fold/call action per own hand after observing the bet. Neither may optimize separately per joint deal. Enumerating each player's pure hand-contingent policies on tiny games must reproduce U and L, and fixed-policy value must lie between them.
4. Full vs reduced evaluation: block sums preserve values for every lifted block-constant strategy. Full U is at least reduced U; full L is at most reduced L. Identity grouping reproduces original matrices; coarse labels change policy capacity only. Zero-mass pair entries contribute no artificial tie mass.
5. CFR semantics: zero regret -> uniform action policy; signed vanilla regret accumulation without clipping; player 0 updates against old player 1, then player 1 against newly updated player 0. Updates aggregate chance and opponent reach across hidden deals, exclude own reach. Ordinary per-information-set averaging uses own prior action reach, which is one for both players in this one-decision game, even for player 1 after a zero-probability opponent bet. Compare regret arrays and averages to generic CFR on small games and independently derive the first steps.
6. Bounds vs convergence: gap is (U-L)/2, deviations sum to U-L; both methods' BR gaps must be visible. Finite-iteration results must not be called intrinsic abstraction loss or exact equilibrium. All reported policies must be finite and in [0,1].

## Derived representation invariants

7. Uniform baseline: all C(47,2)=1081 board-compatible own hands, each compared with C(45,2)=990 legal opponents; equity (wins+0.5*ties)/990, double precision multiplication by 200 before int and cap at 199. Verify against baseline code semantics and a distinct evaluator. Raw label identity may be compacted only without changing membership.
8. Declared input selection: SHA-256 ASCII payload exactly as specified, canonical sorted board and holes, digest then hand tuple ordering, distinct player salts. Positive weights are 4 iff polarized equity <=0.2 or >=0.8, otherwise 1. Development board ints decode to the declared boards. No holdout admitted.
9. Range features: conditional opponent probability given own hand after collision removal. Three half-open equity strata except final closed upper edge; three masses sum to one. Win/tie coordinates reflect own perspective for both players and do not exceed corresponding mass. Range equity equals all wins plus half ties, independent of own unnormalized range weight.
10. Capacity fairness: baseline K is occupied groups on positive marginal support per player; both feature methods have exactly K occupied labels. Deterministic highest-weight first seed, subsequent weighted squared nearest-seed distance, ties by canonical hand order; every distinct seed index remains assigned to its own group even for duplicate vectors; 20 weighted Lloyd updates. K=1, K=n, and duplicate-feature cases must remain finite and deterministic.

## Driver, integration, and evidence invariants

11. Bounded declared CLI: CPython 3.14.6, two development board choices, 2..96 hands, uniform/polarized regimes, 1..10000 iterations; default 16 hands/100 iterations. Refuse existing output roots, holdouts, tracing during timing. Review only tiny diagnostics/focused tests; no 96-hand comparison, holdout, training campaign, wrapper, or git mutation.
12. Reproducible artifacts: persist selected hands, weights, joint mass, exact equities, per-method groups, iteration identity, and group/lifted average policies; checkpoint 100/1000/10000 when reached plus requested terminal bound. Evaluate every method on the same matrix. Report raw bounds/deviations and gap distinctly for full and reduced games. Reports must be finite JSON and retain non-improvements. Chip units and fractions of pot are a reporting requirement; omissions are graded by material effect on this development driver.
13. Timing/storage honesty: compilation, grouping, aggregation, cumulative training, and evaluation measured separately; no tracemalloc in timed run; array bytes explicitly exclude process peak, Python/evaluator objects, and transient storage. Timing does not establish playing strength.
14. Output safety/provenance: exclusive creation, source hashes and invocation/environment in start record, successful member hashes in manifest, failure marker on exceptions after start. Multi-case aggregation, campaign plan consumer, formal retention and holdout criteria are deferred, not first-gate defects.
15. Test integration: cases.json insertion registers exactly the new focused test file without unrelated changes; run direct file arguments to avoid journal harness and caches. Target must match the original identity again at review completion.

## Planned evidence

After hashing this inventory: inspect focused tests, test harness configuration and cases.json diff; execute focused tests on Python 3.14.6 with bytecode/cache disabled and output locations confined to this review directory. Independently check tiny nonuniform/tied/card-blocked games and pure-strategy extrema; compare baseline equity membership on a development board; exercise bounded driver using existing focused checks where permitted. Record any remaining gaps explicitly. No material finding without a reachable declared-use example or a falsifier.
