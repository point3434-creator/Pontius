# Fixed-group optimality diagnostic r001 review

One fresh opposing review of the incremental diagnostic. Read identity.json first,
verify all seven members and the current branch/base, then read the pinned spec and
source. Use plan.json to identify the seven computational surfaces and 32 input pins.
Record and hash an independent inventory before test source or author checks.

The prior research is still uncommitted. The new candidate changes only the new
kernel/tool/test/spec, the test registry and byte-preservation attributes. Historical
registry/attribute bytes are preserved under predecessor-metadata/; the previous
holdout computational source, datasets and review evidence have not changed.

Review the one-sided LPs, exact-rational certificate proof and implementation,
independent hand-level responses, group feasibility, interval/gap direction, frozen
inputs, separate cohort reporting, and one-shot bounded execution/failure paths.
Both Python/scientific environment and exact plan/source/input binding must verify.
The exact scope is the encoded one-bet two-player game; do not infer a multiway or
unseen-board claim. No hostile-filesystem or unrelated architecture expansion.

Use only the identity interpreter (Python 3.14.6), -B, no bytecode/cache writes.
Run focused synthetic and two-hand smoke tests; do not run the observed-case plan
or optimize any of its saved cases. Their pinned input files may be read as data.
No dependency installation, source mutation, commit, push or extra reviewers.

Do not open memory files, ledgers, earlier review/disposition/check directories,
research result narratives, or unrelated author/reviewer scratch. Disclose injected
historical context and any prohibited-source exposure; no unsupported cold claim.
Write report, inventory and evidence only to the assigned reviewer scratch, then
reverify identity. One focused followup is appropriate if a material issue is fixed.
