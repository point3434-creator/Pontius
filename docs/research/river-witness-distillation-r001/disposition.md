# Witness distillation r001 disposition

Finalizer readiness: PASS for the bound experiment through the mandatory accounting
recorder. The actual training fit and reserved evaluation require separate one-shot
controller authorization. No invocation, commit or push is authorized by this document.

## Research decision

Use the 48 observed witness-pilot cases on eight boards to fit one fixed quadratic
ridge predictor per seat. Predict the four existing witness advantage features from
the eleven existing range/equity features, without tuning on the reserved panel.
Evaluate 48 cases on eight newly reserved boards with matched grouping capacity.
Compare certified strategic floors against the three existing compressed controls,
the exact control and directly computed witness grouping. Retain every case,
nonimprovements, feature errors, board/regime/pool breakdowns and complete cost.

The fixed recipe tests whether these inexpensive features can recover strategically
useful witness information. A negative result constrains this recipe, not all learned
abstraction. The conditional river-game panel does not establish six-max playing
strength or generalization to all boards. Historical teacher-generation cost remains
separate from the new fitting, prediction and evaluation costs.

## Original review and finding

The original opposing report's Specification: Partial and Engineering: Partial labels
remain unchanged. Its sole F1 finding is accepted: the frozen runner's receipt stops
after its worker, before parent refitting, reconstruction and final output work.
That receipt cannot establish the required whole-invocation duration.

Original report SHA-256:
4dfb29b31c37b737d9cc8db9732a7ba1cb726e46f41f5725f5ef751da8d0a208

Original inventory SHA-256:
427562a8801044e19555408c7c105e0c2fe243b8b2e46f164b6bc117886b6678

The additive accounting recorder measures the unchanged inner command through process
exit and retains its command, binding, captures, status and whole_command_seconds.
It preserves child failure and fails on tested evidence-write or launch failures.
Its preflight and final receipt write are explicitly outside the measured interval.
Worker and whole-command times remain distinct; their difference is not pure parent cost.

The same reviewer returned Pass for F1 closure in both targeted specification and
engineering judgments. This is a focused follow-up, not another cold review. Its
three fresh inert-process tests and independent failure/capture probes passed.
No actual training fit or reserved scoring was used to establish closure.

Follow-up report SHA-256:
c5d01c2622bb80184b78e1a187eed92aa7a6e2d186158b064defe75012f6f04f

The reviewer reported CURRENT.md's then-missing disposition link. This document
resolves that navigation item before delivery; the reviewer did not review its text.
No additional review is needed for this coordinator disposition.

## Evidence and preservation

Author verification: 104 affected tests passed, no failures or skips, and lint passed.
Accounting addition: three inert tests passed and lint passed. The reviewer freshly
ran nine candidate tests plus six independent tests, followed by the three accounting
tests and independent accounting probes. These counts overlap and are not summed.

All seven candidate identity pins, eighteen original packet freeze members, fifteen
plan source/contract pins and sixty-five training archive bindings were preserved.
Both original and follow-up reviewer manifests are checked before byte-exact copying.
Original report and inventory bytes and original Partial labels are retained unchanged.
The computational source, plan, recipe, selected boards and original freeze did not change.

The initial reviewer disclosed general injected project memory context, but did not
open memory files or prior review narratives. Its inventory preceded checks-content
inspection. The follow-up knew the finding and repair request. These qualifications
remain in the reports; this disposition makes no stronger coldness claim.

## Current execution and authority

The only current execution entry is accounting/run-command.txt. The only approval
template to copy is accounting/approval-template.txt. Root templates are preserved
superseded history. Read accounting/contract.md with the current template.

Plan SHA-256:
47b26fc64aa3b2ddb36ebe11a02d16253b240a61672cff162c51af689728e524

Mandatory recorder SHA-256:
c675cbfd5a6d57b7e43144b26ca2e14462cc87b91682c56faf2388595cb25e7f

The proposed invocation fits the fixed models, independently refits them in the parent,
and evaluates 48 reserved cases with 576 new LP calls. Worker limit: 1,200 seconds.
Parent validation and I/O are outside that limit; no RSS cap is claimed. There is no
automatic retry. The bound output and invocation evidence directory must be absent.

After exact controller approval, retain verbatim words and both hashes in
controller-authorization.json. That record is an audit prerequisite, not authentication.
Completion requires successful inner execution and independently verified output
manifest together with the outer receipt and both captures; archive both layers.

At finalization no controller authorization, actual-data fit, reserved score, bound
output or invocation exists. Safe tests used synthetic data or two-hand old-board
smokes. Model quality, full-run runtime and resource feasibility remain unmeasured.

At the next legitimate runner revision, move whole-command accounting into the common
research execution path while preserving this boundary. Do not duplicate this helper
for each future experiment without assessing whether a shared implementation suffices.
