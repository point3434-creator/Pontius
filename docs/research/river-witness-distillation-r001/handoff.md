# Witness-feature distillation r001 review contract

Perform one focused opposing review. No fan-out. Start with identity.json and
recompute its seven pins, the freeze manifest, the 15 plan source/contract pins,
and training bindings. Read docs/research/river-witness-distillation.md, the three
new Python files and unchanged computational dependencies. Seal your own invariant
inventory before opening checks/. Hash-reading evidence during identity checks
does not count as consulting its contents; disclose any other exposure.

Critical questions: training-only targets and model fit; distinct reserved board
suit classes; weights give equal case and board totals; polynomial order and ridge
penalty including unpenalized intercept; feature signs/reach and test-context inputs;
exact capacity matching; test groups precede test teachers; parent reconstructs
training/model and each case; all-case signed summaries and cost boundaries;
missing/corrupt evidence cannot complete. Assess this fixed feature-transformation
pilot, not a claim of unrestricted neural abstraction or full-game deployment.

Allowed historical inputs are the bound training archive's producer plan and case
records, manifests and machine-readable invocation/verification records needed to
verify provenance. Do not open prior narrative reports, reviews, dispositions,
progress.md, INDEX.md, memory files, transcripts, or another reviewer's scratch.
The frozen training files may be read to verify the contract, but do not fit the
actual 48-case training set. No reserved-board equities/groups/LPs or any bound
invocation. Only synthetic regression and two-hand old-board checks are allowed.
Read inherited source directly; do not import another test module's whole suite
unless its execution domain has first been checked against these restrictions.

Python 3.14.6: D:/Pontius/tmp/group-opt-author/venv/Scripts/python.exe.
Use -B -W error::ResourceWarning and pytest -p no:cacheprovider. No installations,
source/metadata edits, commit, push, ledger entry or additional agents. Read-only
Git. All writes exclusively in D:/Pontius/tmp/witness-distill-review-01. Seal
inventory.md and report.md with SHA-256, retain check receipts, and verify identity
again at completion. State specification/engineering verdicts, actionable findings
with reachable falsifiers, test limits and all inherited-context disclosures.

Keep review proportional: another round needs a material unresolved finding or
changed executable behavior, not a documentation advisory. This is setup only;
no authorization exists and no research invocation or actual fitting has occurred.
