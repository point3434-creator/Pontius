# Focused opposing review: witness distillation r001

One actionable finding: the runner does not retain the whole-invocation wall time required by its specification. No defect was found in the fixed regression recipe, training/evaluation information boundary, exact capacity matching, or parent model/case reconstruction within the permitted evidence domain.

**Specification verdict: Partial.** The fitting and evaluation setup passes the focused safe checks. The explicit whole-invocation timing requirement is unmet (F1). Full 48-case fitting and reserved evaluation were not executed, as required by the handoff.

**Engineering verdict: Partial.** The new numerical and integration paths passed 15 focused tests, with no unresolved result-correctness or train/test leakage finding. Retained cost telemetry is incomplete (F1). This is a bounded accounting defect, not evidence of incorrect strategic results, insufficient training strength, or infeasibility. The controller can disposition it without repeating broad inherited review; executable repair warrants only a targeted timing check.

## F1 — P2 / medium, high confidence: retain complete invocation time

Location: `D:/Pontius-worktrees/eval-runner-consolidation/tools/river_witness_distillation.py:176-202`, particularly receipt creation at lines 176-177 and parent refitting at line 183. Requirement: `docs/research/river-witness-distillation.md:101` says to report worker and whole-invocation wall times. Lines 113-115 explicitly place parent reconstruction/refitting and I/O outside the worker timeout.

On every successful invocation, `receipt.json.seconds` is calculated immediately after the worker and output captures, before parent training reconstruction/refit, every evaluation-case reconstruction, final binding check, summary writing and manifest writing. The summary then retains worker-generated stage times and the original worker fitted record. It does not retain parent reconstruction timings or a complete duration. The supplied run-command is a direct Python command; no packet wrapper supplies an outer timing receipt.

Reachable falsifier and fresh evidence: the reviewer executed only the permitted two-hand smoke (one old-board training case and a case on the second original development board). `test_retain_two_hand_smoke_and_time_boundary` in `test_independent.py` measured `execute_plan` externally and inspected all output keys. `timing-boundary.json` records:

- Outer reviewer measurement: **2.40777240000898 seconds**.
- Production receipt: **1.7179629000020213 seconds**.
- Difference: **0.6898095000069588 seconds** outside that receipt.
- Production receipt keys: `exit`, `seconds`.
- No complete-invocation timing appears in the production summary or manifest-listed records. The complete smoke output is retained in `independent-smoke-output/`.

The outer measurement is reviewer-only scratch evidence, not a timing artifact generated by the bound runner. The numerical difference includes entry binding and output reservation as well as the later parent work, so it must not be labeled a pure parent-validation benchmark. The defect is independently established by the source boundary and absent output field; it is not inferred only from timing variance.

Consequence: a future run can report worker cost while omitting the separately required complete execution cost, including parent revalidation/refitting. This does not change model coefficients, grouping comparisons or completion semantics, but prevents complete retained cost accounting under the current contract.

Smallest remediation direction: retain a separately named elapsed duration through successful parent reconstruction/verification and output finalization, or add an explicitly bound outer launcher receipt that measures full successful parent exit and identifies its measurement boundary. Keep the worker receipt distinct. Do not assume such a wrapper already exists. No source or metadata changes were made by this reviewer.

## Identity and provenance

Initial and completion checks match **7 identity pins, 18 freeze entries, 15 plan source/contract pins, and 65 training archive bindings**. `identity-initial.json` and `identity-final.json` have equivalent records with no mismatch; the final check script additionally compares the snapshots. `verify_identity.ps1` retains the final checking recipe. Candidate identity file SHA-256: `a45ed16bb9e0a3d6446fad9e68ece84d4d0fe75abdf72e6ae2c749e2f22d23e4`. Plan SHA-256: `47b26fc64aa3b2ddb36ebe11a02d16253b240a61672cff162c51af689728e524`.

HEAD stayed `1b4d1a0e26cd4da90ff74678de2e48ef53ec5599`; branch is `codex/river-abstraction-holdout`. Read-only Git status showed the same preexisting tracked modifications at beginning and end. The candidate is an uncommitted worktree identity, not a claim that HEAD contains these files. The training binding is rooted at the exact producer milestone; plan-only tests call the production binding checks, confirm 48 training and 48 evaluation cases and 65 bound files, and forbid fitting/equity preparation while doing so. Historical machine-readable invocation verification was inspected for provenance; its old successful reconstruction claim was not substituted for a fresh actual-data reconstruction.

## Requirement-to-evidence results

| Inventory | Evidence and outcome |
|---|---|
| I1 provenance | Pass for current byte identity and production plan bindings: all 105 binding entries match at start and finish; recipe/source/training drift is rejected in focused tests. |
| I2 reserved selection | Pass for outcome-free selection: deterministic 8 unique suit classes, 2 per inherited texture, disjoint from development/holdout and training classes. Only selection metadata was computed. |
| I3 training-only learning | Pass within inspected setup and smoke: load_training reconstructs training case records before fit_models; raw inputs contain range/equity context only; fit_models takes four verified candidate feature columns in inherited bank order. No reserved training fit was attempted. |
| I4 equal case/board weights | Pass: fit_model normalizes each own-hand vector and divides by case count. Independent unequal-row/unequal-total-mass synthetic cases produce the required mean of 6 rather than an example- or mass-weighted mean. The pinned 48-case balanced panel has six cases per board, giving equal board totals. |
| I5 polynomial/ridge | Pass: all 78 basis columns checked in exact order and float64. Independent augmented least-squares solution agrees with the production normal-equation coefficients at absolute tolerance 1e-10. Analytic new tests also check ridge shrinkage and an unpenalized intercept. |
| I6 feature information/sign | Pass on source and old-board checks: inherited range_features transposes both joint and sign for seat 1; band mass/win/tie columns precede range/uniform equity; inputs pass probability/finite validation. |
| I7 target sign/reach | Source-confirmed: inherited seat-0 advantage is bet minus check; seat-1 advantage is call minus fold, weighted by opponent bet reach, and divided by own marginal. Training labels come from this verified inherited proposal. No broad inherited suite was rerun; the planned separate synthetic target enumeration was omitted as unnecessary to the new fitting/integration review. |
| I8 capacity | Pass: predicted groups use baseline occupied capacity per seat and inherited anchors. Additional tied-prediction synthetic check keeps exactly K occupied labels for K=1..6; existing old-board test checks proposal capacities. |
| I9 ordering/reconstruction | Pass in source and permitted smoke: observed fit -> models.json -> evaluation-equity ordering, and predicted groups -> teacher ordering. Parent refits and requires canonical model equality, verifies teachers and students, and rejects corrupt predictions/groups/comparisons/teacher inputs/model/timing. Verification test patches LP entry to raise, showing no optimizer use in case verification. |
| I10 complete summaries | Pass in source and focused tests: inherited exact interval subtraction uses candidate minus control, interval sign determines classification, all records retained, equal board weighting checked, primary nonimprovements retained, oracle and error diagnostics coexist without a gain ratio. Missing/reordered cases are rejected. No reserved endpoint was computed. |
| I11 costs | Partial: named worker stages and historical-label exclusion are clear; 12 LP calls per evaluated case and 576 for the full panel follow the inspected call chain. Whole-invocation timing is absent (F1). No full-scale resource claim is made. |
| I12 completion/failure | Pass for tested paths: successful two-hand subprocess/parent completion, fresh directory collision, worker failure, timeout, corrupt model, drift and partial manifest write are exercised. Failure evidence is retained; successful parent exit plus valid manifest remains required. Arbitrary hostile mutation and power-loss recovery are explicitly outside scope. |

## Fresh execution receipts

Python **3.14.6**, pytest **9.1.1**, NumPy **2.5.2**, SciPy **1.18.0** in the pinned interpreter. All execution used `-B -W error::ResourceWarning`, pytest `-p no:cacheprovider`, `PYTHONDONTWRITEBYTECODE=1`, `PYTHONWARNINGS=error::ResourceWarning`, and one BLAS thread via OPENBLAS/MKL/OMP variables. TEMP, TMP, pytest basetemp, XML, logs, synthetic training and outputs were confined to the exclusive reviewer scratch.

1. New module: `D:/Pontius/tmp/group-opt-author/venv/Scripts/python.exe -B -W error::ResourceWarning -m pytest -p no:cacheprovider D:/Pontius-worktrees/eval-runner-consolidation/tests/test_river_witness_distillation.py --basetemp D:/Pontius/tmp/witness-distill-review-01/pytest-temp --junitxml D:/Pontius/tmp/witness-distill-review-01/new-tests.xml` — **9 passed in 8.11s**; `new-tests-escalated.txt` and `new-tests.xml`.
2. Independent module: same interpreter/options with `-c D:/Pontius-worktrees/eval-runner-consolidation/pyproject.toml`, `test_independent.py`, scratch basetemp and `independent-tests-fixed.xml` — **6 passed in 3.67s**; `independent-tests-fixed.txt` and XML. Checks are full basis order, equal-case weighting, augmented least-squares oracle, tied capacity, formation/persistence ordering and retained two-hand timing smoke.
3. Final `verify_identity.ps1` — successful, all counts above and unchanged snapshots.

Environment incidents are retained and not hidden: the first pinned interpreter launch was denied by sandbox process startup (`new-tests.txt`, exit 101); an approved escalation ran the same bounded checks successfully. The first independent pytest collection selected the enclosing D:/Pontius configuration, whose pythonpath imported the wrong checkout; collection failed with zero tests executed (`independent-tests.txt` and XML, exit 2). Specifying the target worktree configuration fixed the review harness. No production fix or installation was involved. The smoke retention test creates exclusive output paths and is intended for one execution in this scratch; no automatic rerun was made.

After sealing the independent inventory, the reviewer opened the supplied `checks/author-receipt.json`, XML suite counts and lint output. They report 104 passing final checks, an earlier missing-module red phase and an intermediate corrected consumer error. These are disclosed author evidence, not the basis for claiming 104 fresh reviewer tests. Only the 15 focused tests above were freshly run. No inherited test module's whole suite was imported.

## Disclosure, scope and seals

The invariant inventory was sealed **before any checks-directory content exposure**. Hash reads of checks during initial identity verification preceded that seal but did not inspect their contents. Inventory SHA-256: `427562a8801044e19555408c7c105e0c2fe243b8b2e46f164b6bc117886b6678`.

The platform automatically supplied a general Pontius memory summary and user preferences. No memory files were opened or used as evidence, and no prior narrative report, review, disposition, progress.md, INDEX.md, transcript, or another reviewer's scratch was opened. The parent task provided the focused review assignment and handoff path. Mid-review parent messages requested a progress report, emphasized focus on fitting/information boundaries/integration, and said not to assume an absent outer timing record; they supplied no prior verdict or hidden evidence. General code-verification skill/matrix and the subagent exemption in using-superpowers were read. Allowed historical exposure was limited to hashes, producer plan data traversed by current binding checks, and machine-readable training invocation verification.

No fan-out; no actual 48-case fit; no reserved-board equity, grouping, teacher, score or LP; no bound invocation; no broad inherited validation run; no installation; no source/metadata edit; no commit, push, ledger entry or launch authorization. All reviewer writes are under `D:/Pontius/tmp/witness-distill-review-01`. Full-run runtime, memory, numerical trajectory and strategic outcomes remain unmeasured. This review concerns the fixed feature-transformation pilot, not unrestricted neural abstraction or full-game play.

`inventory.sha256` and `report.sha256` seal the two review documents; `review-manifest.json` hashes the retained reviewer artifacts. The report's own SHA-256 is external to avoid a self-referential seal.
