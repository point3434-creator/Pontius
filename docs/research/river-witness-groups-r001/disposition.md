# Witness grouping r001 final disposition

Build readiness: PASS. Specification: PASS. Engineering: PASS within this bounded
oracle-assisted development screen. The coordinator read the one opposing report
and accepts its verdict; no material unresolved finding requires a source change.
The original report and sealed inventory are copied byte-exact under reviews/.
Context disclosures in that report remain part of the independence record.

Author verification passed 87 affected tests, zero failures/errors/skips, under
Python 3.14.6 with ResourceWarning promoted to error; Ruff is clean. The checks
include hand-derived action signs and reach weighting, rectangular payoffs,
same-capacity grouping, a known synthetic improvement, corrupt-bank refusals,
the real two-hand producer/consumer path, and failure/completion boundaries.
The review's independent tests and limitations are stated in its retained report.

Accepted nonblocking observation: plan regeneration calls the predecessor's
eight-case binding loop before retaining four development bindings. It therefore
hashes and discards previously observed holdout inputs. The bound runtime's
features, groups and scores use only development cases. Bind the intended four
cases directly at the next legitimate plan-generation revision; this observation
does not affect the bound invocation or warrant a correction round.

The intended information budget is explicit: these are opponent witnesses from
four previously solved controls, including the full-hand control. Previous witness
construction cost is excluded. Any development result is an oracle-assisted
representation probe, not proof of a practical feature generator, equal compute,
generalization, six-max playing strength, or superiority on future cases.

All four candidates and comparisons must be retained, even if they are worse.
There is no parameter search, best-of-many selection, ordinary CFR training, or
saved holdout construction/evaluation in this phase. The plan is fixed at four
development cases, 96 hands per seat, eight new LP calls, and existing group counts.

The approved-run request remains separate from this build disposition. The worker
limit is 120 seconds; each LP has five seconds and 10,000 iterations. Parent
verification and I/O are outside the timeout. No RSS cap is imposed. No saved-case
grouping or optimization has been executed in preparing this candidate.

The old computational source, packet evidence and three milestones are preserved.
Test-registry and attribute extensions retain their prior bytes in
predecessor-metadata/. The research overview links to this packet. The freeze
manifest and identity still verify. The output directory is absent.

Next gate: one controller-approved invocation of plan.json under its SHA-256,
using approval-template.txt. This disposition authorizes no run, commit, push,
deployment, adoption or subsequent experiment. All work remains uncommitted.
