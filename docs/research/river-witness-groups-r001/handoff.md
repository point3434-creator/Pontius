# Witness-action-value grouping r001 review contract

One focused opposing review of this uncommitted candidate. No reviewer fan-out.
Use identity.json to bind the seven candidate/metadata/plan files to the existing
worktree and base commit. Recompute source and input pins from plan.json.

Read identity.json and the governing docs/research/river-witness-groups.md, then
the three new Python files and reused source dependencies. Write and hash your
own requirement/invariant inventory before opening checks/ or any author receipt.
Only then inspect checks/ and run proportionate synthetic checks. The plan binds
the sixteen development input files, four witness cases and their manifest/plan;
you may inspect those allowed inputs and their source contracts. Do not construct
new groups or invoke an LP on any saved 96-hand case. Existing unit tests and the
two-hand fixture are permitted; do not run any bound development invocation.

Do not open prior reviews, dispositions, any progress.md or INDEX.md, conversation
transcripts, memory files, unrelated scratch or another reviewer's inventory.
Disclose any inherited context or accidental exposure. The caller can coordinate
this task but its conclusions are not review evidence. Report specification and
engineering verdicts separately, with reachable falsifiers for material findings.

Important invariants: correct action signs and reach normalization; the intended
opponent vectors from each asymmetric certificate; deterministic grouping at the
unchanged occupied capacity; input-to-witness binding; full-hand floor certification;
all-case signed aggregation without winner selection; no false completion on
worker, certificate, missing-file or evidence-write failures. Assess the stated
oracle-information and prior-compute limitations rather than assuming a practical
or equal-compute feature generator. Only development cases are consumed.

Runtime: D:/Pontius/tmp/group-opt-author/venv/Scripts/python.exe, Python 3.14.6 only.
Use -B and pytest -p no:cacheprovider with ResourceWarning promoted to error.
No dependency installation, source/metadata edit, commit, push, new agent or
research invocation. Use read-only Git. Write all reviewer work exclusively to
D:/Pontius/tmp/witness-groups-review-01. Preserve report.md and inventory.md there
with hashes and disclose all temporary checks performed. Verify identity again
before finishing. No source fixes or result publication by the reviewer.

Keep the review proportional: a further correction round requires a material
unresolved finding or changed executable behavior, not a documentation advisory.
The predecessor metadata copies preserve the previous test registry and attributes;
the old computational sources, packet evidence and all three milestones are intact.
