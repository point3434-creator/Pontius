# Witness-action-value grouping r001: independent opposing review

No material unresolved finding was found in the frozen candidate. Specification verdict: **PASS for the reviewed development-screen implementation**. Engineering verdict: **PASS for the scoped source, admission, synthetic and two-hand verification**. These verdicts do not authorize or claim successful execution of the bound 96-hand development plan. No saved 96-hand grouping or LP was performed.

## Bound identity and independence

Target root: D:/Pontius-worktrees/eval-runner-consolidation. Base commit: 1b4d1a0e26cd4da90ff74678de2e48ef53ec5599. Branch: codex/river-abstraction-holdout. Both remained unchanged at final verification. The seven identity file pins, ten plan source/document pins, sixteen development input pins, four witness-case pins, witness plan and witness manifest pins matched before and after testing. Predecessor tests/cases.json and .gitattributes hashes matched; comparison against those copies proved the prior registry/attributes remain intact with only the new witness-group entries added.

- identity.json SHA-256: c74d6809404b26554b6cbf5fb13dda0e92a7e3b54be280f9d54073cfa6e28edf
- Bound plan SHA-256: 9581a2a8e8d74d9d66e31abed59a61cff7625b769be4521a303b0c343f0ed855
- Inventory SHA-256, sealed before author checks and unchanged afterward: 56284849ef50df572aacace5cd27205790b4063f4487d55c6447a29f9924bbf3
- pins-before.json and pins-after.json both SHA-256: 054baa6ea7c90ef53656456c86cdd51a6e08e3fd23afa0fbec77fff873dc7f71

I started with handoff.md, then identity.json and the governing document, followed by the three new Python files and reused source dependencies. I wrote and hashed inventory.md before opening checks/ or the author receipt. The author receipt, final-tests.xml and lint.txt were inspected afterward, but their claims were not substituted for fresh checks. No parent conclusions were used as evidence. A later parent status message reported the freeze and author/preservation checks; I continued to rely on my own pins and execution results.

The task inherited general system/developer/user context, including the automatically supplied memory summary, but I did not open or use memory files, prior reviews/dispositions, progress.md, INDEX.md, transcripts, unrelated scratch, another reviewer's inventory or holdout case contents. Process-only code-verification skill files were read. using-superpowers was read and explicitly exempts dispatched subagents. An accidental read of a nonexistent skill-adjacent path returned no content. The root AGENTS.md lookup also returned no content. Read-only Git status showed changed filenames, including README.md, without opening their contents. These disclosures are also captured in the sealed inventory.

## Requirement-to-evidence assessment

| Inventory | Assessment and fresh evidence |
|---|---|
| R1-R2, R14, R16 | Pin recomputation and read-only Git identity passed before and after. Read-only tool.verify_bindings admitted the real plan while linprog and witness anchored_clusters were patched to raise if invoked. Synthetic wrong producer input binding was rejected specifically as 'witness uses different case inputs'. |
| R3, R10 | The run consumes the four declared development case IDs in fixed order; summary requires the complete ordered grid. Existing signed aggregation test used mixed positive/negative results and obtained the exact rational all-case mean, rejecting a missing case. Four independent interval boundary cases covered lower, higher, touching and exactly equal intervals; touching/equality remained overlapping. |
| R4-R5 | Six independent scalar/Fraction action-feature comparisons on dense 2x3, 3x2 and 3x3 games passed with nonzero check payoffs and nonuniform marginals. Caller features scaled with betting reach and became zero at zero reach. Existing hand-derived, rectangular and mass-scaling tests also passed. |
| R6-R8 | A separately constructed 3x3 certificate bank with different two-group partitions distinguished the intended seat0.call and seat1.bet columns from wrong-seat alternatives. Both expected feature arrays matched. Repeated proposal calls with linprog forbidden were identical and retained two occupied groups per seat. Existing tied-feature, invalid policy, zero marginal, method-order and unequal-capacity rejection checks passed. Source inspection confirms the unchanged anchored_clusters uses marginal weights, stable argmax/argmin hand-index ties, permanent anchors and 20 Lloyd updates. |
| R9 | Independent pure response enumeration exactly matched both asymmetric certificate endpoints on the synthetic candidate. Pairing seat0.bet with seat1.call achieved exactly the claimed full-hand exploitability upper endpoint. Both rational widths were <=1e-8. Existing known-floor, certificate tamper, invalid-policy and unsuccessful-solver tests passed. |
| R11-R13 | The focused suite and a separate retained two-hand smoke exercised genuine producer, worker and parent execution. Tampered features, groups, score, worker failure, timeout, missing output and summary/manifest write failure were rejected. An independent partial manifest-write injection left failed.json with complete=false and raised to the caller; the partial manifest was invalid JSON. Output-directory reuse was rejected. |
| R12, R15 | Fresh runtime: Python 3.14.6, NumPy 2.5.2, SciPy 1.18.0 at the required interpreter. Static review confirms eight planned sequential LP calls, per-LP 5-second/10,000-iteration limits, worker timeout 120 seconds, and BLAS environment assignment before NumPy import in the driver path. The document and summary explicitly limit interpretation to an oracle-assisted observed development screen with prior witness construction cost excluded. |

Primary source anchors: src/pontius/river_witness_groups.py:15 (formulas), :37 (bank verification and witness selection), :67 (signed intervals), :82 (parent reconstruction); tools/river_witness_groups.py:59 (admission/binding), :117 (all-case summary), :144 (fixed worker loop), :155 (failure/completion contract); src/pontius/river_group_optimality.py:45 (exact response certificate), :72 (LP limits), :108 (floor verifier); src/pontius/river_abstraction_study.py:161 (unchanged clustering). Paths are relative to the bound target root above.

## Findings and reachable falsifiers

There are no material findings requiring a correction round. The implemented incentives have the correct player signs and normalization. Each bank policy is reverified against its reconstructed game and original group constraints before use. The caller's decision reach is retained; no conditional-on-facing-a-bet division is introduced. Full-hand best responses remain the certification domain. Signed comparisons use every case and all four controls, with range_equity primary and range_response secondary; no winning subset or fallback is selected.

A nonblocking scope observation: tools/river_witness_groups.py:39 calls the predecessor make_plan before slicing its cases to four. Static inspection shows that regenerating a development plan therefore also reads/hashes the already observed holdout input files in the predecessor's eight-case loop. Those hashes are discarded from the new plan; they provide no candidate features, grouping or score, and the frozen plan's admission/run reads only the four development cases plus the permitted producer metadata. This does not invalidate the reviewed bound invocation under the explicit no-held-out-feature/group/score contract. A future cleanup could bind only the intended four cases to remove that unnecessary plan-generation dependency. Falsifier: with base.bind_case replaced by a recording stub, a development make_plan records eight original-case bindings before the slice. I did not execute this path or open holdout inputs in this review. This observation does not warrant another review round.

Reachable falsifiers for the main positive conclusions are retained in independent_checks.py and the sealed inventory: a wrong asymmetric seat must disagree on the dedicated fixture; a wrong action sign, omitted check payoff or reach renormalization must disagree with scalar sums; incorrect full-hand floor arithmetic must disagree with enumerated responses or the paired-policy upper endpoint; rebinding a witness to different input hashes must reject; a partial evidence-write failure must not return successful completion.

## Commands, results and temporary work

All Python execution used D:/Pontius/tmp/group-opt-author/venv/Scripts/python.exe with -B -W error::ResourceWarning. Temp and TMP were directed under this review directory for tests; PYTHONDONTWRITEBYTECODE=1 and PYTHONWARNINGS=error::ResourceWarning propagated to children. Pytest plugin autoload was disabled and the cache provider was disabled. No installation occurred.

1. verify_pins.py pins-before.json: exit 0, matching pins, exact environment and identity; saved-case LP/group calls blocked. An initial sandboxed launcher attempt returned Access is denied before Python started; the identical required-interpreter command succeeded after approved escalation. No automatic approval rejection occurred.
2. From the target root, -m pytest -q -p no:cacheprovider tests/test_river_witness_groups.py tests/test_river_group_optimality.py --basetemp=D:/Pontius/tmp/witness-groups-review-01/pytest-temp --junitxml=D:/Pontius/tmp/witness-groups-review-01/focused-tests.xml: exit 0, **22 passed in 10.48 seconds**, no skips, failures or ResourceWarning errors. Logs are focused-tests.txt and focused-tests.xml.
3. independent_checks.py: the first scratch-only attempt had an unclosed-parenthesis SyntaxError before execution. I corrected only my scratch oracle and retained the failed log as independent-checks.txt. The corrected run exited 0; independent-checks-corrected.txt and independent-checks.json record six scalar checks, two asymmetric-source checks, two enumerated certificates, the paired-policy upper endpoint, four interval boundary checks, a producer-input mismatch rejection, real two-hand worker/parent success and a partial-manifest failure rejection. This script used ten synthetic 3x3 LP calls plus the permitted two-hand producer/candidate executions; none used saved 96-hand cases.
4. verify_pins.py pins-after.json: exit 0 and byte-identical JSON evidence to pins-before.json. Inventory hash unchanged.
5. Read-only Git rev-parse, branch, status and targeted metadata diff; PowerShell file reads/hashes and scratch-only writes. No commit, push, source/config/metadata changes, new agents or result publication.

Retained reviewer work: inventory.md, report.md, verify_pins.py, independent_checks.py, before/after pin JSON, pytest XML/text, failed and corrected independent-check logs, independent-checks.json, genuine two-hand files beneath independent-smoke/, and the partial-write failure evidence. TemporaryDirectory fixture directories used by existing tests were created under temp/ and cleaned by their existing test context managers. A final seal.json hashes every retained file except itself; the report hash is also returned to the caller.

Selected evidence SHA-256:

- focused-tests.xml: d830bab869c3beac0e2178b284d7982cfaa8c2e1173c7865dc1c7183aa2243b7
- independent-checks.json: 0d5d64b26a9e9eec2240312aa767d10587b1e1ed00a399bcd5e0a381fde03379

## Limits of the verdict

No bound development invocation, saved 96-hand feature construction, clustering or LP was run. Consequently this review measures no candidate improvement, actual four-case runtime, memory peak or probability of completing within the worker limit. Existing 96-hand certificates are pinned and their verification path was reviewed; I did not rerun all retained 96-hand certificate arithmetic. Correct failure behavior is required if that later arithmetic or an LP fails.

Exact rational certification concerns the encoded binary64 payoff coefficients, not ideal pre-rounding probabilities. The reused underlying card evaluator is not independently reimplemented here. The exact control supplies full-hand oracle information, and its historical construction cost is outside the screen limits. Matched occupied groups therefore do not establish equal information or compute, practical feature generation, fresh holdout performance, six-max strength or BB/100 improvement. Parent reconstruction/certification and I/O are outside the worker timeout; no RSS cap exists. Hard kills, power loss and hostile concurrent mutation remain outside the stated recovery contract.

The author receipt reports broader historical milestone preservation and 87 passing author tests; I did not expand this review into prohibited packets or a full historical artifact audit. Fresh preservation evidence here is limited to the allowed pinned inputs/dependencies and predecessor metadata. The separate controller approval remains necessary for execution of the frozen development plan.