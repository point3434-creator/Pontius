# Fresh-board witness pilot r001 review contract

One focused opposing review. No reviewer fan-out. Start with identity.json,
recompute the seven candidate/plan/metadata pins and the freeze manifest, then
read docs/research/river-witness-pilot.md and the three new Python files. Inspect
the unchanged computational dependencies pinned by plan.json as needed. Record
and hash your own requirement/invariant inventory before opening checks/.

Review board selection without outcome peeking; suit-equivalence exclusion;
deterministic repeated hand pools; balanced board-level aggregation; correct
fresh witness generation and certificate reconstruction; unchanged feature recipe
and per-case group capacity; failure/missingness and evidence-write completion.
The pilot deliberately makes descriptive claims only. Assess that scope rather
than assuming 48 independent boards, a power guarantee or natural-play weights.

Do not open earlier reviews, dispositions, progress.md, INDEX.md, memory files,
conversation transcripts, unrelated scratch or another reviewer's inventory.
Disclose any injected context or exposure. Existing dependencies and their
governing source contracts are allowed. Do not inspect earlier result archives
or evaluate any fresh-board equities, groups or LPs. Tests may select boards and
holdings without scoring. Existing synthetic/two-hand old-board tests are allowed.
No bound pilot invocation or new 96-hand solve, even as rehearsal.

Python 3.14.6 only: D:/Pontius/tmp/group-opt-author/venv/Scripts/python.exe.
Use -B, -W error::ResourceWarning and pytest -p no:cacheprovider. No installation,
source or metadata edits, commit, push, ledger write or additional agent.
Read-only Git. Write exclusively to D:/Pontius/tmp/witness-pilot-review-01.
Seal inventory.md and report.md there with SHA-256; verify candidate identity
again before finishing. State separate specification and engineering verdicts,
reachable falsifiers for findings, test evidence and the context disclosure.

Keep this review proportional. Another round follows a material unresolved
finding or changed executable behavior, not a documentation advisory. Execution
still requires a separate one-shot controller approval; no approval file exists.
