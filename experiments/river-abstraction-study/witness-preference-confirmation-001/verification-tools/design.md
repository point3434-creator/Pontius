# Frozen ordinary-preference fresh-board confirmation

Authority: "Freeze design and run next text". Interpret text as test in context:
freeze the ordinary-preference recipe and run the next fresh-board confirmation.
One invocation; no training, model selection, commit, push or playing-policy adoption.

Candidate: the two ordinary_preference seat models retained in witness-preference-001,
with every coefficient unchanged. Pin the complete producer training.json and extract
the exact model records into candidate.json before board selection. Same eleven raw
inputs, quadratic basis, four sigmoid outputs, fixed anchored clustering and per-case
occupied capacity as uniform_equity_200/range_response/range_equity. No oracle targets
or fresh witness policies enter candidate inputs. No weighted classifier or alternative
learned candidate in this confirmation. Baselines: range_response (primary), range_equity
(secondary) and exact individual hands (numerical lower reference). All methods solve
the same two-player, one-bet river game with 96 holdings per seat and unchanged ranges.

Deterministic board rule: seed 'witness-preference-confirmation-001', SHA-256 order of
all 52 cards for attempts 0..9999; take the first five, sort, classify with the existing
four texture categories. Take the first four boards per category, rejecting suit-
isomorphic duplicates and every board in this study's original development/holdout,
witness-pilot and witness-distillation panels. Pin and retain that full exclusion list.
Use the original hand_pool algorithm unchanged, three pools per board and uniform/
polarized regimes. Four boards x four textures x three pools x two regimes = 96 cases.
Selection can compute card identities/textures only, not equities, predictions, payoffs
or scores. Freeze plan, candidate, source and input digests before first fresh scoring.

Primary endpoint: equal-weight mean of the 16 board mean grouping-floor differences,
ordinary_preference minus range_response. Each board equally averages its six cases;
the texture strata are equally represented. Directional replication passes only if
the upper endpoint of this signed numerical interval is below zero. No minimum effect
size is invented after the run. Separately report a sensitivity flag requiring negative
mean upper endpoints in each of four texture strata and every leave-one-board-out panel.
Neither flag is a population confidence statement or automatic adoption decision.
Report mean magnitude, all board/pool/regime/texture results, signed case counts,
worst regressions and independent bettor/caller contributions. No omitted cases, sample
size extension, alternate seed, threshold tuning or favorable-subset selection.

Four methods x 96 cases x two asymmetric LPs = exactly 768 new LP calls. Existing
per-LP five-second and 10,000-iteration limits. Fresh full-hand certificates establish
the encoded game's reference and verify the ordering of grouping floors numerically.
Worker saves each case with complete raw inputs, candidate features, groups, policies,
certificates, signed comparisons and stage times. Count actual LP calls and forbid
model fitting. Parent rebuilds every game/feature/group and verifies all certificates
with optimization disabled. A separate audit uses scalar sigmoid predictions and exact
rational floor/comparison/aggregate/seat-effect arithmetic without refitting or LPs.

Analytic scalar-prediction, tie/capacity and signed-endpoint tests precede the freeze.
Board selection must be reproducible, suit-isomorphism invariant and disjoint from
the complete pinned old panel. All producer bytes remain unchanged. No test scores
are available at freeze time; no scoring rehearsal on this fresh panel.

Python 3.14.6 / NumPy 2.5.2 / SciPy 1.18.0, one BLAS thread, no allocation tracing.
600-second worker limit, parent verification outside it, no RSS cap. Exclusive output
reservation, checked captures and exit, pre/post pin validation, no automatic retry.
Record worker and worker-plus-parent times and preparation/prediction/clustering/solve
stage times. Prediction/grouping times assume already loaded coefficients and prepared
board equity/raw features; they are not end-to-end live decision latency.

This is fresh-board evidence under a fixed synthetic, equally stratified distribution,
not real reach-weighted poker, full-pool, full-game or six-max strength. Numerical
certificate widths describe optimization error on encoded binary64 payoffs, not board
sampling uncertainty. Retain all outcomes as witness-preference-confirmation-001 and
preserve all twelve prior milestones. No cold opposing review is claimed.
